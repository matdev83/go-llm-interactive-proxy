## Session — 2026-07-10T12:30:00.000Z

Phase 3 of the accounting-authority decision-history remediation: re-threaded settlement/release deltas into `appendDecision` so decision history explains per-mutation outcomes.

### Changes

- `pkg/lipsdk/controlplane/accounting_authority.go`: added `Released`/`Overage`/`Adjustment` (int64, `json:"...,omitzero"`) to `AccountingDecisionRow` after `Remaining`, and documented that they carry the settlement/release delta for the mutation (zero for reserve decisions). Additive, backward-compatible (existing rows omit them via `,omitzero`).
- `internal/infra/usageauthority/authoritystore/store.go`: re-added `released, overage, adjustment int64` params to `storeCore.appendDecision` (after `amount domain.Amount`) and assigned them to `Row.Released/Overage/Adjustment`; re-threaded the 4 call sites — reserve-deny and reserve-success pass `0, 0, 0`; settle passes the already-computed `released, overage, adjustment`; release passes `released, 0, released` (mirrors the limit-row `row.Adjustment += released`).
- The durable store needs no separate change: it persists `decisionRecord.Row` as JSON via the mutation log and reloads it through `json.Unmarshal`, so the new fields flow through `storeCore` automatically.

### Tests (TDD)

- `contract/contract.go`: new `testDecisionRowRecordsSettlementDeltas` in `RunSuite` (runs against both memory and SQLite via the shared `Factory`): settle-with-overage asserts `Overage==overage, Released==0, Adjustment==-overage`; settle-with-release asserts `Released==released, Overage==0, Adjustment==released`; release asserts `Released==released, Overage==0, Adjustment==released`; reserve (allow and deny) asserts all three are `0`.
- `pkg/lipsdk/controlplane/accounting_authority_test.go`: `TestAccountingDecisionRowDeltasOmitZero` asserts the new fields serialize with `,omitzero` (present when non-zero, absent when zero) and round-trip — matching the existing `WindowStart/End/ResetAt` `,omitzero` contract.
- `append_decision_test.go`: updated the two direct `appendDecision` boundary calls to the new signature (`0, 0, 0` for the reserve cases).

### Verification

- `go test ./internal/infra/usageauthority/authoritystore/contract ./internal/infra/usageauthority/authoritystore ./pkg/lipsdk/controlplane`: PASS
- `go build ./...`: PASS
- `go test ./internal/core/usageauthority/... ./internal/archtest/...`: PASS
- gofmt clean on all changed files.
