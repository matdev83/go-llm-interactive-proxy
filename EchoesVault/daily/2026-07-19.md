# 2026-07-19

### Scratchpad - Cursor SDK live-bridge harness repair (attempt 1)

- REJECT repaired: removed production `live_bridge.go` harness; orchestration lives in `_test.go` + `cursorsdk_live_bridge` tag.
- Drives production `backendRuntime.Open` → `*RunStream` (not `BridgeAgentClient`); pin via `--version`; fake `$auto` run IDs; create-count accumulates across kill/reap; prompt capture for full-prompt proof.
- Live residual: `canonical_rebootstrap` **blocked** without fake instrumentation; cancel **blocked** if provider finishes too fast; MCP/sandbox-required **blocked**.
- Scripts: Windows inherits Process else User/Machine key into process only; clears process copy after run.
- Bookkeeping fix: `quietCancelWindow` no longer Recv-with-short-deadline (closes `RunStream`); peer leases must match kill generation; live test emits one stdout JSON; rebootstrap blocked without instrumentation covered by test.

### Scratchpad - hard_bridge_restart peer-proof vs restart failure

- Root cause: harness accepted any peer Recv error but recorded restart Open/drain `err.Error()` bare, so live `cursor_sdk_bridge_exited: bridge run ended before terminal` looked like peer conflation; natural peer EOF before kill was also treated as pass path.
- Fix (test-only): `classifyLiveBridgePeerErr` / `liveBridgeHardRestartOutcome` — peer `ErrBridgeExited` / “bridge run ended before terminal” = proof; restart errors prefixed `restart:`; early/natural peer end = **blocked**.
- Proof: unit classify/outcome PASS; fake content+kill PASS; natural peer end BLOCKED; full `cursorsdk`+`fakebridge` PASS; safe live-bridge script BLOCKED without opt-in. Arch `TestLineComplexityBudgets/internal/core` pre-existing unrelated fail.

### Scratchpad - hard_bridge_restart REJECT repair

- Removed 40ms sleep; fake `HoldUntilCancel` Path notify + content-active gate; kill-side `Process.Kill` count on gen1 proc; spontaneous pre-kill death BLOCKED; typed `ErrBridgeExited`/code only; peer watches joined.
- RED: loose-string proof accepted; GREEN: focused+full cursorsdk/fake + TestCursorSDK arch PASS; safe live-bridge BLOCKED.

### Scratchpad - production gen isolation after hard_bridge_restart

- Live: `restart: cursor_sdk_bridge_exited: bridge run ended before terminal` after correct peer proof — next Open contaminated by leftover gen1 `runSub` (same run ID / claimed) when `waitProc` skipped `closeRuns` after gen advanced; death path `InvalidateAll` could wipe gen2.
- Fix: `runSub.generation`; `closeRunsForGenerationLocked`; eager close on kill; arm/Subscribe replace stale gen; `InvalidateGeneration` from death coordinator; late death still hooks when gen advanced.
- RED→GREEN: `generation_isolation_test.go`; full `cursorsdk`+`fakebridge` PASS; cursorsdk arch PASS; `go vet` PASS; `-race` skipped (Windows cgo); `TestLineComplexityBudgets/internal/core` pre-existing.

### Scratchpad - reuse wait-only + live-bridge -v

- Node reuse: two `run.wait()` on same agent (no `run.stream`); timeout isolated on `reuse.wait1`; text streaming unchanged.
- Scripts: `go test -v` in live-bridge ps1/sh so passing JSON is visible; `TestLiveBridgeScripts_UseVerboseGoTest`; safe BLOCKED unchanged.
- Proof: Node typecheck+105 tests+build PASS; Go LiveBridge/scripts PASS; safe script BLOCKED.

### Scratchpad - process-death TerminalErr stamp + harness diag

- `closeWithErr` stamps typed `BridgeExited`(+sanitized stderr) on gen-scoped run close from `waitProc`/`failGeneration`/`KillGeneration`; `Close` still plain `closeRunsLocked`.
- Harness: per-proc `Waited()`; restart fail detail `ready/gen/gen2_waited/stderr_class`.
- Live distinguisher: `gen2_waited=true` ⇒ Node exit; `false`+typed exit ⇒ Go close without Wait; opaque gone if stamp works.
- Proof: focused death/harness + full cursorsdk/fake + vet + arch cursorsdk PASS.

### Durable - credentialed live + bookkeeping (final)

- Node probe: PASS exact SDK 1.0.23, 34 models, deltas, disposed, cancelled.
- Node scenarios: discovery/text/safety_off/configured_mcp/cancel/shutdown PASS; reasoning SKIP (no thinking-delta); sandbox-required BLOCKED Windows; hard-restart/rebootstrap hooks BLOCKED; reuse last credentialed run (before text-only reuse prompt update) timed out and **has not been revalidated** afterward — do **not** claim Node reuse live-passed.
- Go live-bridge text-only run (`go test -v` JSON): parent status **blocked** exit 0; pin/discovery/canonical single terminal/cancelled terminal/no later content/shutdown PASS; hard restart BLOCKED (active text-only peers could not be held); rebootstrap/MCP instrumentation BLOCKED; sandbox-required BLOCKED. Do not mark hard restart/rebootstrap/Node reuse live passed.
- Repaired earlier: official anonymous variants; generation-scoped run isolation; typed process-exit diagnostics; text-only prompts avoid upstream pwsh spawn.
- Docs/tasks/EchoesVault updated sanitized; experimental rollout accepted; measured replacement/dogfood still blocked. Offline: golangci/gofumpt clean, cursorsdk+fake PASS. No commit. No model IDs/keys/paths/prompts.

### Durable - quality-gate repair (PreviewAttempt/SkipEvidence + fakebridge cancel)

- Root causes: incomplete clamp-preview API (`PreviewAttempt`/`SkipEvidence` missing); `metering/plane` unwired duplicate helpers + core line budget; fakebridge single-threaded `WaitForFile` blocked `run/cancel` under parallel-race load.
- Fix: `AdmissionInput.SkipEvidence` + adapter `PreviewAttempt` (EstimateOnly+SkipEvidence, strip holds); wire `plane` into `metering_egress`; raise `internal/core` budget with rationale; fakebridge reader-path cancel + cancel-clears-on-new-send.
- Proof: `make quality-checks` PASS; `make test` PASS; `make lint` 0 issues; `lipstd check-config` valid. No commit.

### Durable - race tests → HoldUntilCancel + clean QA

- Race proofs migrated off `WaitForFile` to `HoldUntilCancel` + active-notify (notify before Respond); history test uses `OnAgentSend` hold then normal finish for re-bootstrap.
- Ambient `LIP_TEST_POSTGRES_*` User/Machine DSNs caused schema-drift QA fails; with process DSNs cleared: `make quality-checks`/`make test`/`make qa`/`make lint`/`govulncheck` PASS. No commit.

### Durable - PR-readiness review follow-ups (false-green closure)

- Wired V-15 clamp preview end-to-end: `AttemptCoordinator.Preview` + `invokePreviewAttempt`; runtime bounded loop (≤4) after `openCall` assembly / before freeze; UA adapter Money on spend clamps; real Service+evidence sink `TestPreviewAttempt_RealServiceSkipsEvidenceSink`.
- Wired `operatorUsageForFinalize` into operator settle/egress (lastAuthorityUsage → seenEvents → empty UsageDelta shell); customer path keeps `usageEvidenceOrEmpty`.
- `cancelLosers` ignores only `context.Canceled`; `DeadlineExceeded` surfaces (`parallel_race_cleanup_test.go`).
- Postgres residual (not a PR code failure): ambient Neon with drifted `metering_facts` unique (`(store_id, source_event_key)` vs code `UNIQUE(source_event_key)`). Baseline migration is `CREATE TABLE IF NOT EXISTS` so it will not rewrite an already-wrong constraint; fix by re-provisioning a fresh test DB or applying a one-off constraint migration outside this PR. Default `make qa` with process DSNs cleared skips optional Postgres via `SkipUnlessPostgres` — legitimate. Do not run ambient-DSN QA against remote Neon from this agent (destructive risk).
- Proof (process Postgres DSNs empty): `make quality-checks`/`make test`/`make lint`/`make qa` PASS; race stress `-count=3` PASS; `lipstd check-config` valid. Residual design gap only: reservation-stage exposure-changing clamp not already previewed → compensate+reject not yet implemented. No commit.
