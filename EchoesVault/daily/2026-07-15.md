### Scratchpad — 2026-07-15T07:20:17.829Z

- Remediated all reviewer findings for the active `dual-plane-economics-and-concurrency-foundation` spec using issue-family workers plus independent review/repair rounds.
- Authority: external attempt providers run without built-in usage authority; request/attempt settlement preserves provider ownership, full evidence, bounded cleanup contexts, panic isolation, and retryable per-provider completion.
- Economics/snapshots: injected raters now drive admission, settlement, and output-limit clamp quotes without catalog fallback; public runtime exposes explicit bounded snapshot refresh and degraded/unavailable source posture.
- Metering/concurrency: FE/BE quantities are counted and merged before legal admissions; fact replay conflicts reject; aggregate money/quantity arithmetic is checked and currency-safe; multi-rule leases are independently owned/renewed/released; durable renew CAS cannot resurrect released leases.
- Fresh verification passed: `go test -count=1 ./...`, `make quality-checks`, `make parity-checks`, focused remediation tests, and `testdata/enterprise_module` tests. PostgreSQL integration contracts were not run because no DSN was available.

### Scratchpad — 2026-07-15T07:29:36.924Z

- Follow-up code review found two remaining issues and both were remediated: frontend-egress recorder append failure now prevents request authority settlement and preserves retryable state; `SnapshotController.Refresh` is serialized so concurrent refresh invocations cannot publish out of order.
- Added regressions `TestSettleRequestAuthority_AppendFailureRemainsRetryable` and `TestSnapshotController_ConcurrentRefreshesPublishInInvocationOrder`.
- Fresh verification passed: affected package tests, `make quality-checks`, `go test -count=1 ./...`, and `git diff --check`.

### Scratchpad — 2026-07-15T07:57:28.289Z

- Diagnosed PostgreSQL release-gate stall/failures with `LIP_TEST_POSTGRES_DSN`: supplied Neon DSN used the transaction-pooler host (`-pooler`), which does not preserve schema/search-path and transaction behavior required by schema-isolated multi-statement contract tests. Symptoms included missing freshly seeded rows/tables and a false release/renew resurrection failure.
- Verified isolated authority tests and the complete authority package pass serially; the lease race failed through the pooler but passed through the corresponding Neon direct endpoint.
- Derived the direct host in-process without printing credentials and ran `make GO_TEST_FLAGS='-count=1 -parallel=1 -timeout=10m' test-authority-postgres`; authoritystore, leasestore, and journalstore all passed. No production-code fix was needed.

### Scratchpad — 2026-07-15T08:05:00.000Z

- Fixed Bugbot finding: malformed admit decisions (unknown Kind) no longer discard claimed reservation/lease handles before reverse compensation.
- `invokeAdmit*` now returns the decision with the validation error; request/attempt/lease coordinators reverse-compensate claimed holds (and prior stack on fail-closed).
- Verification: `go test ./internal/core/authoritycoord/ ./internal/core/runtime/`.

### Scratchpad — 2026-07-15T11:54:40.240Z

- Completed transaction-pooled PostgreSQL remediation for dual-plane authority, concurrency leases, and metering journal.
- Added explicit `direct|transaction_pool` connection modes and `auto_migrate|verify_only` schema modes; pooled auto-migration fails validation.
- Added build-local sanitized `db.PoolRegistry`, shared non-owning store handles, failed-build cleanup, explicit admin migration, and read-only schema verification.
- Centralized runtime PostgreSQL lifecycle orchestration and moved migration dispatch into `internal/infra/dbmigrate`; `lipstd migrate` remains thin CLI glue.
- Replaced `search_path` test isolation with unique IDs and targeted cleanup; pooled tests enforce no runtime DDL/session state and normal `-parallel=8`.
- Added explicit `LIP_TEST_POSTGRES_RUNTIME_IS_POOLER=1` attestation so a direct DSN cannot satisfy the pooled release gate.
- CI now provisions direct PostgreSQL plus PgBouncer transaction mode and routes direct/migration/pooled gates to their correct endpoints.
- Reviewer lifecycle findings fixed: registry pools are not locally closed on schema errors, failed builds close registry pools, auto-migrate uses a separate admin handle, non-registry verify-only paths retain pool closers, and all registry parameters are explicit rather than variadic.
- Fresh verification passed: `go test -count=1 ./...`, `make quality-checks`, direct and attested pooled PostgreSQL gates, and `git diff --check`.

### Scratchpad — 2026-07-15T16:15:00.000Z

- Applied concurrency-review fixes to `db.PoolRegistry` (TDD): `Close(ctx)` with cancellable gate wait (cancel still closes snapshot), `beforeGateWait` hook replacing sleep-based Close tests, per-registry `closePool` seam, and `Stats` snapshot-outside-lock.
- Build failure cleanup uses startup context; runtime dispose closer uses `context.Background()`.
- Verified: `go test ./internal/infra/db/ -run PostgresPoolRegistry`, focused runtimebundle `Pool|PostgresStore|MeteringPool`. `-race` skipped on Windows (cgo toolchain failure).

### Scratchpad — 2026-07-15T16:30:00.000Z

- Implemented golang-performance review fixes for dual-plane Postgres pools: fail-closed `max_open_conns` when any store is postgres; one capped admin migrate pass per DSN at Build; `lip_postgres_pool_max_open_connections` plus counter-typed wait metrics; EchoesVault/docs sizing guidance.

### Scratchpad — 2026-07-15T14:45:44.901Z

- Simplified `validatePostgresPoolBound` to inspect the already-validated raw `DatabaseConfig.MaxOpenConns`, eliminating a second `ParseDatabasePoolSettings`/mode-validation pass during `Validate` while preserving validation order. Verified with `go test ./internal/core/config ./internal/archtest` and `git diff --check`.

### Scratchpad — 2026-07-15T18:46:26.407Z

- Issue #146 foundational inventory/registry: fail-soft aggregation, Discovery/OperationalError contracts in `pkg/lipsdk/modelinventory`, `BuildResult` + per-backend diagnostics in `internal/core/modelregistry`.
- LoadModels errors and empty snapshots omit backends; structural invalid rows/prefixes still fail Build. Cache startup marks `DiscoveryStatusCached`; refresh with zero usable models retains last-good active registry.
- Runtimebundle cold-start test updated for empty-registry success. Verified: `go test ./pkg/lipsdk/modelinventory ./internal/core/modelregistry` and `TestBuild_modelRegistry` in runtimebundle.

### Scratchpad — 2026-07-15T20:13:00.000Z

- Live OpenCode smoke testing with `openai-codex:gpt-5.6-luna` showed that newer Codex streams separate thoughts with native `reasoning_summary_part.added` / `summaryPartAdded` events and distinct summary indexes, without HTML comment markers.
- Both Codex adapters now feed those native boundaries into the shared stateful sanitizer, preserving exactly one newline across summary parts while remaining idempotent with marker boundaries and adjacent newlines.
- Verification passed: focused backend tests, `make quality-checks`, `make test-unit`, and a repeated live Luna stream that rendered separate reasoning headings and completed successfully.

### Scratchpad — 2026-07-15T21:30:00.000Z

- Wave 1 #146 hardened: parent `ctx` cancel/deadline during `LoadModels` is fatal for `Build`/`Runtime.Start`; per-backend `FetchTimeout` remains fail-soft.
- `ErrorProvider.LoadModels` honors canceled context; `BuildResult.Discoveries` and `Runtime.Discoveries()` return defensive non-nil copies.
- Added cancel/timeout/cache-zero discovery tests. Verified: `go test ./pkg/lipsdk/modelinventory ./internal/core/modelregistry ./internal/infra/runtimebundle`.

### Scratchpad — 2026-07-15T21:45:00.000Z

- Issue #146 AGY discovery: `agy models` live `modelsProvider`; pretty NativeID ↔ canonical `vendor/model` map; unknown names omitted with warnings; discovery failure → `ErrorProvider` (no hardcoded inventory).
- `ResolveModel`/`BuildCommand` forward exact pretty `--model`. `NewWithStarter` deterministic (`ErrorProvider` or injected `Config.Inventory`).
- Verified: `go test ./internal/plugins/backends/agycliacp ./internal/standardplugins`.

### Scratchpad — 2026-07-15T21:55:00.000Z

- AGY #146 review repair: restored connector comments; `Config.Inventory` docs; immutable `knownModelTable` + lookup helpers; `ErrUnknownModel` rejected in Open before spawn and in BuildCommand.
- Open-level tests prove unknown starts=0 and known forwards exact pretty `--model`. Verified: `go test ./internal/plugins/backends/agycliacp ./internal/standardplugins`, `git diff --check`.

### Scratchpad — 2026-07-15T22:10:00.000Z

- Issue #146 Cursor ACP + Codex App Server: `agent [-e ep] --list-models` live Provider + concurrent modelIndex; CanonicalID strips leading `cursor-`; unknown rejected before spawn; no hardcoded fallback.
- Codex App Server advertises catalog slugs only when `SourceDiscovered`; `CodexModelCatalogSource` threaded via BackendFactoryDeps/runtimebundle/standardplugins.
- Verified: `go test ./internal/plugins/backends/cursorcliacp ./internal/plugins/backends/codexappserver ./internal/core/codexcatalog ./internal/standardplugins ./internal/pluginreg ./internal/infra/runtimebundle`.

### Scratchpad — 2026-07-15T22:15:00.000Z

- #146 targeted repair (cursorcliacp/agycliacp only): successful LoadModels atomically REPLACES allowlist (empty clears); static YAML override via SetModelInventory + LoadModels constrains Open before spawn.
- Cursor: rewrite stale-preservation test; refresh-shrink + override Open tests; BuildCommand rejects empty; resolveNativeModel nil-safe.
- AGY: per-backend tracking inventory + active index; knownModelTable conversion-only; known-but-unadvertised rejects before start; `agy models` stderr bounded/sanitized with stable OperationalError code; standardplugins uses SetModelInventory.
- Verified: `go test ./internal/plugins/backends/cursorcliacp ./internal/plugins/backends/agycliacp ./internal/standardplugins -count=1`, `git diff --check`.

### Scratchpad — 2026-07-15T22:20:00.000Z

- #146 Codex App Server targeted repair: per-backend `trackingInventory` + concurrent `modelIndex`; successful `LoadModels` atomically REPLACES allowlist; `openai/auto` always allowed; catalog slugs only when SourceDiscovered (fallback/nil → auto-only inventory+allowlist).
- `Open` rejects unknown/unadvertised before ProcessStarter; YAML `models` via `codexappserver.SetModelInventory` (not wholesale ModelInventory replace); standardplugins source-matrix + override tests.
- `discoveriesFromCache`: configured backend with zero cached rows → DiscoveryStatusEmpty + ErrorCodeEmpty (rows remain Cached).
- Verified: `go test ./internal/plugins/backends/codexappserver ./internal/core/modelregistry ./internal/standardplugins ./internal/pluginreg ./internal/infra/runtimebundle -count=1`, `git diff --check` clean on touched files.

### Scratchpad — 2026-07-15T22:30:00.000Z

- #146 integration surfaces: `GET /v1/models` (OpenAI list from live `ModelRegistryRuntime`, ids `<backend-instance>:<canonicalID>`, owned_by=kind); protected `diagnostics.model_registry_path` discovery DTO (no native IDs/raw errors).
- Mounted in stdhttp before frontends; config path overlap + protected posture updated. Docs: config.yaml comments + python-to-go-migration Diagnostics.
- Verified: `go test ./internal/stdhttp ./internal/core/modelregistry ./internal/core/config ./internal/infra/runtimebundle ./internal/plugins/frontends/... ./internal/standardplugins ./internal/pluginreg -count=1`, `git diff --check` clean.

### Scratchpad — 2026-07-15T22:35:00.000Z

- #146 HTTP surfaces reshaped: `NewModelRegistryHandler` → `GET /v1/models`; `NewModelRegistryStatusHandler` via `model_inventory.diagnostics_path` (model_catalog-style, not diagnostics.model_registry_path).
- Mount `/v1/models` after frontends; live Runtime reads. Verified scoped go test + frontend compile + diff check.

### Scratchpad — 2026-07-15T22:40:00.000Z

- #146 final remediation: `codexProtocol.ResolveModel` uses `resolveAllowedModel` so thread/start+turn/start get mapped NativeID (`openai/friendly` → `actual-native`); unit+integration tests prove wire capture.
- stdhttp `/v1/models` now encodes `modelregistry.BuildOpenAIModelsList` (removed duplicate builder); Cursor SetModelInventory comment corrected; root `config.yaml` gained `model_inventory` diagnostics block; Encode errors left as `_` (headers already written).
- Verified: `go test ./internal/plugins/backends/codexappserver ./internal/core/modelregistry ./internal/stdhttp ./internal/standardplugins ./internal/infra/runtimebundle ./internal/plugins/backends/cursorcliacp -count=1`, `git diff --check` clean.

### Scratchpad — 2026-07-15T22:50:00.000Z

- Archtest fix for #146: moved OpenAI /v1/models DTO/helper from internal/core/modelregistry → internal/stdhttp/openai_models_list.go (unexported); deleted core openai_list.go.
- Raised internal/core budget 49750→49950 for live modelregistry inventory/runtime only; updated hexagonal baseline to allow stdhttp→modelregistry.
- Verified: go test ./internal/archtest -run 'TestLineComplexityBudgets|TestHexagonalMigrationBaselineMatchesGoList' -count=1, go test ./internal/core/modelregistry ./internal/stdhttp -count=1.

### Scratchpad — 2026-07-15T23:05:00.000Z

- #146 fail-soft inventory validation: per-backend row invalidity (empty CanonicalID/NativeID, malformed vendor/model, forbidden route-qualifier) omits ALL rows from that backend atomically; ErrorCodeInvalidInventory=`invalid_inventory`; siblings keep advertising; all-invalid cold start publishes empty active registry; invalid refresh with no usable rows retains last-good; config/prefix/provider/parent-cancel remain fatal; cache malformed rows still rejected.
- Verified: `go test ./pkg/lipsdk/modelinventory ./internal/core/modelregistry ./internal/infra/runtimebundle -count=1`, `make test-unit`, `gofmt`, `git diff --check` clean.

### Scratchpad — 2026-07-15T23:20:00.000Z

- Bugbot fix: `modelinventory.AcceptedInventory` + Runtime `syncAllowlists` after every published snapshot so cache-hit Start hydrates ACP allowlists without remote `LoadModels`.
- `Build` clears/`AcceptInventory`s validated rows so invalid inventory cannot leave stale/partial backend allowlists; ACP `trackingInventory` implements the sink.
- Verified: focused + package tests (`modelregistry`, ACP backends, `standardplugins`, `runtimebundle`, `stdhttp`), `go run` cache-hydration check, `git diff --check` clean.

### Scratchpad — 2026-07-15T23:45:00.000Z

- #146 comment: ACP CLI / Codex App Server model enumeration gated to `access.mode` single_user (empty/default = single_user); only enabled loaded backends are built/inventoried.
- `loadCodexModelCatalog` runs only when single_user AND enabled consumer (`openai-codex` / `openai-codex-app-server`); multi_user skips discovery/fallback at composition root; local-only backends still rejected by existing security policy before factory/inventory.
- Test seam: `TestingOptions.CodexCatalogLoad`; regressions in `codex_catalog_access_test.go` + `codex_catalog_attach_test.go`.
- Verified: focused runtimebundle gate tests, `make test-unit`, `gofmt`, `git diff --check`, `./bin/lipstd.exe check-config -config ./config/config.yaml`.

### Scratchpad — 2026-07-15T23:55:00.000Z

- #146 catalog gate tightened: `loadCodexModelCatalog` requires single_user + Enabled + Codex consumer factory ID + `pluginreg.Registry.HasBackend` (registered/loaded).
- Added `HasBackend`; threaded Registry from `buildModelRuntime`. Unregistered enabled consumer: catalog loader 0 calls; Build still fails (missing profile).
- Verified: `go test ./internal/pluginreg ./internal/infra/runtimebundle`, `make test-unit`, `gofmt`, `git diff --check`.

### Scratchpad — 2026-07-16T00:05:00.000Z

- Bugbot fix: when refresh retains last-good after empty/invalid Build, call `syncAllowlists(r.All())` so ACP allowlists match the retained registry (Build may have cleared via LoadModels/`AcceptInventory(nil)`).
- Regressions: `TestRuntime_RetainLastGoodResyncsAcceptedInventoryAfterInvalidRefresh`, `...AfterEmptyRefresh`.
- Verified: focused + package tests (`modelregistry`, ACP backends), `go run` retain/resync check, `git diff --check`.

### Scratchpad — 2026-07-16T00:30:00.000Z

- Cross-checked antigravity resolution_plan: Finding 1 (shared ModelIndex/TrackingInventory) real — extracted to `acp` with `AcceptInventory` (plan omitted it). Finding 3 collapse kept `syncAllowlists` (plan would have dropped it). Findings 2/4/5 applied; 6–8 left alone.
- `SetModelInventory` now lives only in `acp`; ACP CLI factories use `applyConfiguredTrackingModelInventory`.
- Verified: focused packages + `make test-unit` + `make quality-checks` + smoke `go run` (TrackingInventory + SetModelInventory + LoadModels).

### Scratchpad — 2026-07-16T00:50:00.000Z

- Bugbot fix: ACP allowlist no longer advances ahead of published registry. `TrackingInventory.LoadModels` fetches only; `AcceptInventory` (Runtime.publish/syncAllowlists) commits. Removed premature Build `acceptInventory` calls.
- Regression: `TestRuntime_RefreshDoesNotAcceptUntilPublish`, `TestTrackingInventory_LoadModelsDoesNotUpdateIndex`.
- Verified: focused packages, `make test-unit`, `make quality-checks`, smoke `go run` (LoadModels does not advance; AcceptInventory does).

### Scratchpad — 2026-07-16T01:00:00.000Z

- Concurrency low-severity fixes: unified `published` atomic (registry+snapshot); discoveries commit inside `publish` (not ahead of Build); allowlist sync stays after Store (never ahead of registry). `CanonicalFallback` documented immutable after `NewModelIndex`.
- Verified: `go test` modelregistry/acp/runtimebundle/stdhttp/backends/standardplugins.

### Scratchpad — 2026-07-16T01:05:00.000Z

- Context low fixes: `diagnosticsMount` no longer stores `LogCtx` (ctx passed as param); `TrackingInventory.LoadModels` rejects nil ctx before inner.
- Verified: `go test ./internal/stdhttp ./internal/plugins/backends/acp`.

### Scratchpad — 2026-07-16T01:10:00.000Z

- Structs/interfaces low fixes: `noCopy` on `ModelIndex`/`TrackingInventory`; `NewModelRegistry*Handler` return concrete `*ModelRegistry*Handler` (with `var _ http.Handler` checks).
- Verified: `go test ./internal/plugins/backends/acp ./internal/stdhttp`.
