### Scratchpad — 2026-07-15T07:20:17.829Z

- Remediated all reviewer findings for the active `dual-plane-economics-and-concurrency-foundation` spec using issue-family workers plus independent review/repair rounds.
- Authority: external attempt providers run without built-in usage authority; request/attempt settlement preserves provider ownership, full evidence, bounded cleanup contexts, panic isolation, and retryable per-provider completion.
- Economics/snapshots: injected raters now drive admission, settlement, and output-limit clamp quotes without catalog fallback; public runtime exposes explicit bounded snapshot refresh and degraded/unavailable source posture.
- Metering/concurrency: FE/BE quantities are counted and merged before legal admissions; fact replay conflicts reject; aggregate money/quantity arithmetic is checked and currency-safe; multi-rule leases are independently owned/renewed/released; durable renew CAS cannot resurrect released leases.
- Fresh verification passed: `go test -count=1 ./...`, `make quality-checks`, `make parity-checks`, focused remediation tests, and `testdata/enterprise_module` tests. PostgreSQL integration contracts were not run because no DSN was available.

### Scratchpad — 2026-07-15T07:29:36.924Z

- Follow-up code review found two remaining issues and both were remediated: frontend-egress recorder append failure now prevents request authority settlement and preserves retryable state; `SnapshotController.Refresh` is serialized so concurrent refresh invocations cannot publish out of order.
- Added regressions `TestSettleRequestAuthority_AppendFailureRemainsRetryable` and `TestSnapshotController_ConcurrentRefreshesPublishInInvocationOrder`.
- Fresh verification passed: affected package tests, `make quality-checks`, `go test -count=1 ./...`, and `git diff --check`.

### Scratchpad — 2026-07-15T07:57:28.289Z

- Diagnosed PostgreSQL release-gate stall/failures with `LIP_TEST_POSTGRES_DSN`: supplied Neon DSN used the transaction-pooler host (`-pooler`), which does not preserve schema/search-path and transaction behavior required by schema-isolated multi-statement contract tests. Symptoms included missing freshly seeded rows/tables and a false release/renew resurrection failure.
- Verified isolated authority tests and the complete authority package pass serially; the lease race failed through the pooler but passed through the corresponding Neon direct endpoint.
- Derived the direct host in-process without printing credentials and ran `make GO_TEST_FLAGS='-count=1 -parallel=1 -timeout=10m' test-authority-postgres`; authoritystore, leasestore, and journalstore all passed. No production-code fix was needed.

### Scratchpad — 2026-07-15T08:05:00.000Z

- Fixed Bugbot finding: malformed admit decisions (unknown Kind) no longer discard claimed reservation/lease handles before reverse compensation.
- `invokeAdmit*` now returns the decision with the validation error; request/attempt/lease coordinators reverse-compensate claimed holds (and prior stack on fail-closed).
- Verification: `go test ./internal/core/authoritycoord/ ./internal/core/runtime/`.

