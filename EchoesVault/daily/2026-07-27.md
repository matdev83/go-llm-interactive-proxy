### Scratchpad — 2026-07-27 backend-connector-plugin-architecture local closeout

- Hybrid backend connector plugin architecture work is locally complete on Windows for this PR branch: essential builtins stay static; optional backends are executable gRPC connectors under `connectors/` / `connector-support/` (ADR 0008).
- Local gates observed on Windows: `make backend-plugin-release-gates`, `make test-unit`, lint 0 issues, `make quality-checks`, vuln scan. Do not invent merge or multi-OS CI pass evidence.
- `make qa` hit an external Postgres timeout (environment/external), not treated as connector-plugin failure evidence.
- **Human decision (2026-07-27):** no macOS host available — macOS-native tests intentionally skipped locally; CI `macos-latest` workflows remain the native evidence source after push (`acp-process-tree.yml`, `backend-plugin-cross-platform.yml`, `backend-plugin-release-gates.yml`, plus related security/race jobs). Tasks/blockers stay open; no false “macOS passed” claims.
