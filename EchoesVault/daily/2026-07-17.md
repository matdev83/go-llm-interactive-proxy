### Scratchpad — 2026-07-16T22:17:16.163Z

- Reviewer cleanup Phase 1 completed: runtimebundle no longer writes secret-guard composition outputs back into ExtensionsOptions. Added nested SecretGuardInputs for intentional composition seams and a frozen extensions.SecretGuardPlane carried explicitly into RequestRuntimeSnapshot; executor consumes the plane accessor and Built inventory comes directly from the composition result.
- Phase 1 focused verification passed: git diff --check and go test for internal/core/extensions, internal/infra/runtimebundle, and internal/core/runtime.

### Scratchpad — 2026-07-16T22:31:11.870Z

- Reviewer cleanup Phase 2 completed: validated secret-guard blocks now return a first-class SecretGuardBlockInfo with nil stage error; the executor owns final denial via a secret-safe DenialError method.
- Provider error paths no longer inspect untrusted Decision contents for outcome/findings/scan-limit labels; they emit generic failure telemetry only. Focused extensions/runtime tests and diff check passed.

### Scratchpad — 2026-07-16T23:04:53.536Z

- Reviewer cleanup Phase 3 completed: block/redact scan-limit is now a validated OutcomeBlock with nil provider error and follows normal quarantine/audit/denial flow; log scan-limit remains OutcomeLog and continues.
- Redact evaluation now operates on a cloned Call and commits only after a complete successful scan, preventing partial live-call mutation on scan limit or unsupported JSON block.
- Added real shipped-feature runtime coverage for block/redact quarantine, unchanged blocked call, one audit/decision/scan-limit/quarantine metric, no traffic/backend; log continuation remains active. Removed an initial unsafe reflection-based test hack and fixed runtime composition to carry the injected observer naturally.
- Focused plugin/SDK/extensions/runtime/runtimebundle tests, JSON fuzz, and diff check passed.

### Scratchpad — 2026-07-16T23:14:59.191Z

- Reviewer cleanup Phase 4 completed: executor block handling now delegates to one applySecretGuardBlock pipeline. Quarantine attempt classification, one quarantine metric, committed-only A-leg cancellation, one audit event, audit precedence, storage mapping, and final policy denial are ordered in one place.
- Added precedence/count coverage for skipped, missing-session, store failure, committed quarantine, fail-closed audit precedence, best-effort fallback, and committed quarantine plus audit failure. Removed raw quarantine store error logging and the exported metrics trampoline; SecretGuardAudit.Emit provides symmetric event emission.
- Focused secrets-guard/extensions/runtime/runtimebundle tests, CLI check-config, and diff check passed.

### Scratchpad — 2026-07-16T23:23:21.676Z

- Reviewer cleanup Phase 5 completed: memory, SQLite, and Bun Quarantine methods moved from oversized store.go files into adapter-local quarantine.go files. Added pure domain QuarantinePlan and safe audit constants; all adapters validate deterministic non-zero At input, preserve first-event-wins/idempotency, and use plan fields while retaining adapter-specific locking/transactions/SQL.
- Strengthened domain/app/store contract coverage for invalid input and repeated same/different event provenance. Focused domain/app/store/adapter tests, tagged Bun integration command, and diff check passed; external Postgres execution may still be environment-skipped and will be reported conservatively.

### Scratchpad — 2026-07-17T00:14:45.456Z

- Reviewer cleanup Phase 6 completed: secret-guard source/audit/inventory composition now lives in runtimebundle, while internal/infra/secretaudit contains only the structured log delivery adapter. ComposeRuntimeConfig remains the registration authority; duplicate local registration detection and audit re-decode were removed.
- Removed the secretsguard.New identity constructor and stale package/helper files. PolicyProvider.Authenticate no longer mutates request context; middleware invokes a narrow PolicyProvider success-attacher only after TypePrincipal, preserving current credential matcher behavior without adding capability to AuthenticationResult or custom providers.
- Consolidated issue #151 production helpers to restore architecture line budgets without raising thresholds. Focused runtimebundle/auth/plugin/core/secure-session tests, architecture tests, and diff check passed.

### Scratchpad — 2026-07-17T00:30:57.203Z

- Final independent review found one readiness false-positive: guards with no SecureSession manager reported secret_guard_quarantine ready although runtime could only skip quarantine. Fixed readiness registration to require both guards and quarantine capability; added runtimebundle tests for disabled/no-manager, ready/manager, and unavailable/latched-fault states.
- Fresh post-fix verification passed: diff check, focused runtimebundle/runtime/arch tests, and full go test suite.

### Scratchpad — 2026-07-17T06:44:10.643Z

- Evaluated three residual reviewer notes. Kept SQLite/Bun quarantine SQL intentionally adapter-local because shared transition policy already lives in domain.PlanQuarantine; rejected a generic SQL abstraction. Kept broad pre-existing sqlite/store.go decomposition out of issue #151 scope because Quarantine is already extracted.
- Applied the one worthwhile cleanup: flattened runSecretGuardStage to error-first, nil-block-second, apply-block-last control flow. Runtime/extensions/architecture tests and diff check passed.


### Scratchpad — 2026-07-17T06:56:00.000Z

- `/simplify` on `feat/secrets-blocking-redaction-151` applied mechanical cleanups (mode shadowing bugfix, metrics/action dedup, hot-path guard sort/clone reduction, ChainObservers dead var, RedactBytes match-before-copy, scanLimitDecision, memory nextAuditSeqLocked). Focused package tests + `lipstd inventory` with secrets-guard example passed.
- Skipped larger items documented as handoff below (not implemented). Full detail also in agent chat response of same session.

#### Handoff — skipped simplify follow-ups

Context: after three read-only reviewers (quality/performance/reuse), parent applied only behavior-preserving small fixes. Items below were explicitly deferred.

---

**S1 — Streaming JSON scan / avoid full unmarshal+remarshal**
- Where: `internal/plugins/features/secretsguard/json_redact.go`, `scan.go` (`touchJSON` / `scanJSONPayload` / `redactJSONPayload`)
- Why skipped: redesign of correctness-critical path; docs freeze sorted-key deterministic JSON walk, number preservation via `json.Number`, unsupported-token block on redact. Not a mechanical simplify.
- Full scope if done: (1) scan-only token stream via `json.Decoder` matching only string/key/scalar tokens without `map[string]any` tree; (2) redact copy-on-write or in-place patch then remarshal only when mutated; (3) preserve `UseNumber`, sorted object key order for findings, invalid-JSON opaque byte fallback, unsupported-token → block on redact; (4) parity tests + fuzz (existing `fuzz_test.go` / frontend matrix); (5) update `docs/secrets-guard.md` JSON section if semantics change. Estimate: multi-day, own task/spec slice.

**S2 — Defer `lipapi.CloneCall` until first redact mutation**
- Where: `internal/plugins/features/secretsguard/guard.go` `evalRedact`
- Why skipped: Phase 3 intentionally clones before scan so scan-limit / unsupported-JSON never partially mutate the live call. Changing that risks aliasing bugs.
- Full scope: read-only scan on live call OR scratch buffer; clone or COW only when first mutation required; guarantee rollback on scan-limit/unsupported-token; extend `secret_guard_real_feature_test` / redact tests for partial-failure invariants. Estimate: 0.5–1 day with careful tests.

**S3 — AC matcher: dense goto table + incremental overlap resolution**
- Where: `internal/core/secretsguard/aho_corasick.go`, `matcher.go` `selectMatches`
- Why skipped: algorithmic optimization needing fuzz/bench gates; correctness (longest match, alias retention) is security-sensitive.
- Full scope: precompute fail-free goto at build; optional online non-overlapping winner without full sort; keep `BenchmarkMatcher_*`, fuzz, known-prefix redaction tests green; document complexity. Estimate: 1–2 days.

**S4 — `ScanString` without `[]byte` allocation**
- Where: `matcher.go`, `credential_matcher.go`
- Why skipped: small win vs JSON cost; needs unsafe/stringdata or string-native AC loop + race/ASAN comfort.
- Full scope: string-backed `findAll`/`ScanString`; keep `ScanBytes`; bench allocs; Windows CI. Estimate: few hours.

**S5 — Default nil `SecretGuardEnvironment` → `osenv.Process{}`**
- Where: `secret_guard_runtime.go`; `internal/infra/osenv/environ.go` exists but unwired
- Why skipped: **behavior change** — today nil env ⇒ empty single-user catalog; wiring Process loads real environ at build. Inventory `catalog_entry_count: 0` on example configs may be current intentional test/inject seam.
- Full scope: decide product default (Process vs require explicit inject); wire in `buildSecretGuardRuntime` only for single-user+enabled; update bootstrap/serve; tests that assume empty catalog; README/docs/examples; confirm multi-user still never reads env (D4). Estimate: half day + product decision.

**S6 — Audit slog duplicate findings payload**
- Where: `internal/infra/secretaudit/slog.go` (`safeFindings` + `findingsAttr`)
- Why skipped: log **shape** change; operators/log pipelines may key on either field; D9 allows high-cardinality audit fields.
- Full scope: pick one representation (prefer summary group for D8/D9); update `slog_test.go`; note breaking change for log consumers; optional dual-write deprecation. Estimate: 1–2 hours + consumer check.

**S7 — SQLite/Bun shared quarantine SQL helper**
- Where: `adapters/sqlite/quarantine.go`, `adapters/bunstore/quarantine.go` (+ AppendAudit duplication)
- Why skipped: earlier Phase 6/residual review kept adapter-local SQL; `domain.PlanQuarantine` already shares policy. Cross-adapter SQL layer is a deliberate architecture choice, not a simplify.
- Full scope: extract per-package `insertAuditInTx` first (safe); optional shared SQL const package only if both adapters stay statement-identical; keep atomic status+audit tx; storecontract quarantine tests. Estimate: 0.5 day for per-adapter helpers; more for shared SQL.

**S8 — `EnsureContextIdentity` copy attribution + credential matcher**
- Where: `internal/stdhttp/auth/middleware.go`
- Why skipped: may be intentional (matcher is request-scoped secret-bearing); copying into child contexts needs security review; tests may rely on stripped attribution.
- Full scope: mirror Principal/Scope for `IngressAttribution` and `RequestMatcher` when absent on child; audit all `EnsureContextIdentity` call sites; tests for decode/sub-context; confirm no secret lifetime extension beyond request. Estimate: few hours + security pass.

**S9 — Readiness quarantine `StoreBacking` / enforcement scope from secure-session store**
- Where: `readiness_report.go` hardcodes `EnforcementScopeAdvisorySingleProcess`, empty backing
- Why skipped: reporting-only enrichment; need map secure-session store config → backing like metering.
- Full scope: plumb store kind from cfg/Built; `EnforcementScopeForStoreBacking`; update readiness tests/golden if any. Estimate: 1–2 hours.

**S10 — scan.go location `fmt.Sprintf` + per-part `mergeFindings` map churn**
- Where: `scan.go`, `json_redact.go` walks
- Why skipped: micro vs JSON tree cost; easy to get wrong on location stability (docs require stable paths).
- Full scope: allocate location only on hit; single dedup pass at end of `scanCall`; keep location string stability tests. Estimate: few hours.

**S11 — Inventory nested `instance_id` / flat `InventoryExtras` fields**
- Where: `inventory_extensions.go`
- Why skipped: JSON shape / operator inventory contract; removing nested `instance_id` is wire change.
- Full scope: embed `InventorySecretGuard` in extras or drop redundant field with inventory golden updates (`cmd/lipstd/testdata/.../inventory.golden.json`). Estimate: 1 hour if golden-only.

---

Recommended next priority if pursuing performance: **S1** then **S2**. Product correctness default: **S5** needs an explicit decision before code. Do not mix these into leftover #151 polish without a scoped task.
### Scratchpad — 2026-07-17T07:25:41.990Z

- Reassessed the /simplify S1-S11 handoff against issue #151, current code, and docs. Conclusion: the list overstates substantial required work. Two concrete pre-approval fixes remain: standard lipstd serve leaves SecretGuardEnvironment nil so single-user startup env enumeration produces an empty catalog, and secretaudit emits two JSON attrs with the same `findings` key.
- S2 eager CloneCall is a guard-private transactional copy preserving no-partial-mutation semantics, not a runner/evidence copy; treat as optional perf/doc clarification. S1/S3/S4/S10 require profiling before optimization. S7/S8/S11 should remain unchanged absent evidence. S9 readiness backing accuracy is a small reporting follow-up, not a runtime blocker.

### Scratchpad — 2026-07-17T08:23:29.292Z

- Orchestrated separate S2/S5/S6/S9 implementations with TDD and independent review. S2 now scans read-only before cloning, cloning only when findings require a transactional redact; no-match and matcher-error/cancellation regressions added. S5 standard BuildBootstrap serve now injects osenv.Process through an explicit per-call unexported implementation, with single-user positive and multi-user/disabled/inspect zero-read tests. S6 now emits one detailed findings array plus a distinct deterministic finding_summary object. S9 now reports effective secure-session store backing and derived enforcement scope for quarantine readiness.
- Repaired follow-up issues: removed a mutable bootstrap env factory global in favor of explicit per-call dependency; consolidated runtimebundle one-use helpers so the unchanged 5300-line architecture budget passes. Independent review found no remaining actionable issues.
- Fresh completion verification passed: focused secretsguard/secretaudit/runtimebundle/core runtime/arch tests, full go test ./... -count=1, go build ./..., go vet ./..., diff check, and lipstd single-user secrets-guard config validation.

### Scratchpad — 2026-07-17T16:07:12.245Z

- Completed and independently reviewed Phase 1 tasks 1.1-1.5 for `dual-plane-economics-and-concurrency-production-readiness`.
- Added final-canonical customer evidence, exact backend-exposure rating and clamp preview, explicit metering presence/correlation, and once-only settlement for incurred sequential/parallel attempts.
- Phase 1 integration validation returned GO; `make test-unit`, repeated dual-plane matrix, architecture budget, and CLI smoke all pass.
- Simplified about 152 core lines before ratcheting the documented `internal/core` budget to 57,550 for a measured 57,447 lines.
- Committed as `9f1cfef` and opened Phase 1 PR https://github.com/matdev83/go-llm-interactive-proxy/pull/166. Later spec phases remain pending.

### Scratchpad — 2026-07-17T18:51:00.000Z

- Fast PR CI: removed race + Tier-1 fuzz from `.github/workflows/qa.yml` (keep job name `qa`). Added `.github/workflows/race-fuzz-nightly.yml` (daily 04:17 UTC + workflow_dispatch) with strict race and `FUZZTIME=6s make test-fuzz`. Updated release-gates/README/Makefile/EchoesVault docs for PR vs nightly split.

### Scratchpad — 2026-07-17T19:30:00.000Z

- Phase 1 follow-up: operator evidence completeness + metering/plane extraction.
- Incurred loser/swallowed paths now finalize/egress with observed usage (empty shell only when unobserved).
- Pure dual-plane helpers live in `internal/core/metering/plane`; runtime keeps accumulators/orchestration.
- Verified: `go test ./internal/core/runtime/ ./internal/core/metering/...`, arch line budget, `lipstd --help`.

