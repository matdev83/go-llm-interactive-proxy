### Scratchpad — 2026-07-06T12:00:00.000Z

- Ported OpenAI Codex App Server backend from Python (`openai_codex_app_server.py`) to Go (`internal/plugins/backends/codexappserver/`). This is a local-agent stdio backend that speaks the Codex JSON-RPC 2.0 protocol (initialize → initialized → thread/start → turn/start → item/* → turn/completed), distinct from ACP's session/prompt. Reuses ACP infrastructure types (RuntimePool, WorkspacePolicy, Transport, ProcessStarter) but has its own backend runner, handshake, stream mapper, and server-request handler.
- Added unit tests for three ACP CLI connector packages (`cursorcliacp`, `geminicliacp`, `agycliacp`) — previously zero coverage. Tests cover ResolveModel, HandshakeProfile, CancelProfile, server request handlers, New() factory, and compile-time interface assertions.
- Added Codex App Server integration tests using fakeProcess + custom ProcessStarter to drive the full lifecycle: handshake → thread/start → turn/start → stream notifications → turn/completed → close. Tests cover streaming text, reasoning deltas, approval request handling, model override, and workspace resolution from extensions.
- Fixed JSON-RPC -32601 error propagation in ACP `handleInboundServerRequest`: when a ServerRequestHandler returns an error, a -32601 error response is sent to the agent instead of terminating the stream. Added `replyServerRequestErrorJSON` helper and `captureRPCTransport` test transport.
- Registered Codex App Server in `pluginreg/standard_table.go` and `pluginreg/backends_acp_cli.go` as backend ID `openai-codex-app-server` with model prefix `openai/`.
- Added EchoesVault page `codex-app-server-backend.md` documenting the protocol mapping, handshake flow, event mapping table, and server request handling. Updated `index.md`, `package-map.md`, and `plugin-system.md` with the new backend and ACP CLI connector entries.
### Scratchpad — 2026-07-06T19:22:49.749Z

- Completed orchestration for confirmed thermo-nuclear review issues: shared NDJSON stream/predicate, shared subprocess EnsureProcess scaffolding, ACP CLI normalized connector config, process identity helper cleanup, call extraction dedupe, history transcript wiring, CLI multi-user gate relocation, security profile validation split, BackendAccessAny docs/tests, Codex thread/start shape docs/test, bounded SendJSONRPC discard, and related focused tests/builds.
- Verification evidence: focused Go tests for lipstd/accessmode/runtimebundle/pluginreg/lipsdk/acp/codex/cursor/gemini/agy passed; `go build ./...` passed; `git diff --check` passed. Full `go test ./internal/pluginreg` after final B8 pass timed out twice without diagnostic output, so targeted pluginreg tests were used instead.

### Scratchpad — 2026-07-06T20:45:04.117Z

- Implemented multi-user safety enforcement: backend security profiles now include access scope, runtimebundle rejects local-only backends in multi-user, and standard ACP/OpenAI Codex/Codex App Server connectors are local-only.
- Verified explicit CLI multi-user gate present in lipstd bootstrap and preserving all loopback binds for single_user; focused tests passed for config, accessmode, lipsdk, pluginreg, runtimebundle, and cmd/lipstd.

## Session — 2026-07-06T21:04:43.974Z

Completed multi-user safety enforcement implementation. Added `BackendAccessScope` type to `pkg/lipsdk/backend_security.go`, updated standard_table.go with local-only profiles for ACP/CLI/Codex connectors, added scope enforcement in `security_policy.go`, added CLI `--multi-user` gate in `cmd/lipstd/command.go`, added comprehensive tests across all affected packages. Updated EchoesVault pages (security-auth.md, plugin-system.md), Kiro steering (structure.md, api-standards.md), config docs, and openai-codex-backend.md. All 30 packages pass tests. No remaining steps.

