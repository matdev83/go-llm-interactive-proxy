# 2026-07-16

### Scratchpad — 2026-07-16T00:30:00.000Z

- Hexagonal review remediation (TDD): inject `*slog.Logger` into `modelregistry.Build` / `RuntimeConfig.Log` (remove `slog.Default`); delete `acp.SetModelInventory` — factories/tests use `TrackingInventory.SetInner` only; non-tracking YAML override errors with `not tracking`.
- Unblocked `./...` by finishing incomplete `pkg/lipsdk/metering` modernize (`stringSetEqual` → `slices.Sort`).
- Verified: focused packages (`modelregistry`, `stdhttp`, `runtimebundle`, `standardplugins`, ACP backends), smoke `go run` (injected log + SetInner), `make test-unit`.

### Scratchpad — issue #147 foundation wave

- Added `internal/core/identity`: YAML `identity` policy (upstream UA/OpenRouter URL+title, downstream Server), modes proxy|passthrough|custom|drop; Server forbids passthrough; defaults resolve to `go-llm-interactive-proxy` / project GitHub URL; `BackendOverride` pointer merge (drop ≠ omit).
- Wired root `config.Config.Identity` + LoadFile/Validate defaults.
- `lipapi.Invocation.ClientUserAgent` (`json:"-"`); `identitywire.CaptureClientUserAgent` on all four frontend decoders.
- OpenRouter title tests: blank modern title falls back to `X-Title`.
- Not yet: connector outbound emission, A-leg Server middleware, backend factory override wiring.

### Scratchpad — issue #147 transport + approved connector wave

- Added `internal/plugins/backends/httpidentity`: clone-before-mutate RoundTripper; modes proxy|passthrough|custom|drop; drop suppresses Go-http-client/1.1; background/inventory without call context uses proxy identity under passthrough.
- `identity.WithClientUserAgent` / `CallClientUserAgent`; executor attaches call UA before `Backend.Open`.
- `BackendFactoryDeps.Identity` from `cfg.Identity`; approved factories merge YAML `identity:` override via `resolveIdentityHTTP`.
- Wired: openai-responses, openai-legacy, anthropic, gemini, bedrock, openrouter, nvidia, huggingface. Excluded connectors unchanged (openai-codex vendor UA locked).
- OpenRouter: User-Agent only this wave (no app URL/title redesign). A-leg Server middleware still pending.
- Archtests: allowlist/exclusion + hexagonal baseline + lifecycle skip for httpidentity.

### Scratchpad — issue #147 OpenRouter attribution wave

- OpenRouter B-leg attribution: effective `AppURL`/`AppTitle` FieldPolicy on `openrouter.Config` after global+backend merge; wire `HTTP-Referer` + modern `X-OpenRouter-Title` only (no `X-Title` output).
- Modes proxy|passthrough|custom|drop per field; empty Mode means ModeProxy (aligned with identity defaults). Legacy client-first+`static_*` uses explicit `LegacyAppURL`/`LegacyAppTitle` on `openrouter.Config` (factory sets these for backend `static_*`); factory rejects `static_referer`∩`identity.openrouter.app_url` and `static_title`∩`app_title`.
- `identity.AcceptClientAppURL`/`AcceptClientAppTitle`; `openrouterwire.CaptureHeaders` omits invalid/overlong/control values; categories/metadata unchanged.
- Verified: identity/openrouterwire/openrouter/standardplugins/archtest focused suites; `go run ./cmd/lipstd check-config`; gofmt + `git diff --check` clean.
- Remaining: A-leg Server middleware, docs polish, B2BUA continuity nuances for identity.

### Scratchpad — identity lint cleanup (final make qa)

- Cleared remaining golangci findings on identity wave: checked `http.Flusher` assert; gofumpt blank line; `slices.Contains`/`maps.Copy`/`new(expr)`/range-over-int; revive context-first on `applyUserAgent`/`resolveUserAgent`; SA1012 nil-ctx tests kept with nolint (documented API nil→Background); removed empty branch + unneeded Go 1.22+ loop-var copies.
- Verified: `make lint` → `0 issues.`; focused packages green; `go run ./cmd/lipstd --config config/config.yaml check-config` → configuration is valid; `git diff --check` clean. No commit.

### Scratchpad — golang-structs-interfaces remediation

- Added compile-time asserts: `httpidentity.Transport` → `http.RoundTripper`; `serverIdentityWriter` → `http.ResponseWriter` / `http.Flusher` / `io.ReaderFrom`.
- Removed dual zero-value on `FieldPolicy.Mode` for OpenRouter: empty Mode means ModeProxy; legacy client-first+`static_*` uses explicit `LegacyAppURL` / `LegacyAppTitle` (factory sets when backend `static_*` applies).
- Verified: httpidentity/stdhttp/openrouter/standardplugins tests; `go run ./cmd/lipstd --config config/config.yaml check-config`.

### Scratchpad — golang-testing identity hardening

- `httpidentity`: `goleak.VerifyTestMain`; cancel/concurrency hang-safe waits; worker errors aggregated on test goroutine.
- `stdhttp` 102 holdalive: release-gate barriers (no wall-clock sleeps).
- Parallel-race isolation test: cancellable execute ctx + `sync.Once` release cleanup on failure paths.
- Conformance OpenRouter attribution test tagged `//go:build integration`.
- Coverage: legacy precedence/invalid-capture, empty-Mode UA wire, ModeDrop ServerValue, gemini/openailegacy invalid UA drop.
- Verified focused packages + `go test -tags=integration -run TestOpenRouterBackendFor_defaultProxyAttributionHeaders ./internal/testkit/conformance/` + check-config.

### Scratchpad — issue #147 gap closure

- Spec-bundle: `SpecBundleIdentityScenarios` + `docs/spec-bundle-identity-scenarios.md` + index/archtest locks.
- Tier-1 fuzz: Accept* / ValidateYAML / CaptureClientUserAgent wired in Makefile + release-gates.
- Redirect keep-policy transport test; mutation-kill adversarial tables; docs implementation notes.
- Core line budget 50700→50900 for SpecBundleIdentityScenarios.
- Verified: focused identity packages; new fuzz targets 300–500ms; `make quality-checks`; `make test-unit`; `make parity-checks`; `make lint` (0 issues); `govulncheck` clean; `check-config` valid. `make qa` qa-tests hit unrelated Postgres integration migrate failures (`lip_secure_usage` / bun migrations) outside identity scope. Race skipped on Windows.

### Scratchpad — issue #151 Phase 2 (secrets ingress seam)

- Wired SecretGuards: FeatureBundle → MergedFeatureSurface → ExtensionsOptions → build_extension → RequestRuntimeSnapshot (MaterializeSorted); inventory occupancy stage `secret_guard`.
- Runner `internal/core/extensions/secret_guard.go`: bounded Evaluate, fail-open/closed, panic isolation, D11 no CloneCall, post-chain Validate; block → NewPolicyDeniedError "request blocked; start a new session".
- Inserted after BeginTurn / before captureFrontendIngressBeforeSubmit; best-effort Quarantine (unimplemented ignored).
- Auth: IngressAttribution + opaque CredentialMatcher on AuthenticationResult/context; PeerIP from RemoteAddr; ContextMatcherResolver; no env fallback.
- Runtime RED block test renamed ordinary and green; redact + disabled tests green.
- Verified: gofmt; git diff --check; go build ./...; focused packages (secretguard, httpauth, feature, featurebundle, extensions, runtime, stdhttp/auth, runtimebundle, diag, secretsguard -skip _RED$, cmd/lipstd Inventory/Dogfood). No commit.
- Deferred Phase 3+: catalog/matcher/popular env; Phase 4 feature plugin/scanner; Phase 5 durable quarantine; Phase 6/7 frontend matrix.

### Scratchpad — issue #151 Phase 3 (catalog + source policy)

- Catalog/matcher: BuildCatalog + longest-first Matcher; SDK AsMatcher / NewStaticMatcherResolver; D3 opaque.
- Environment port: Lookup + Snapshot (os.Environ shape); multi-user/disabled never call.
- AccessMode uses internal/core/accessmode.Mode (removed parallel string type).
- Proxy inventory via internal/proxycredentials MatchProxyCredentialName (sparse base_N); keys.go gap-stop unchanged.
- PopularSecretEnvNames + include/exclude; AWS_ACCESS_KEY_ID etc omitted.
- ComposeSource + runtimebundle bindSecretGuardSource → Snapshot.SecretMatcherResolver; executor uses it.
- Verified: gofmt; git diff --check; go build ./...; focused secretsguard/proxycredentials/runtimebundle/extensions/runtime (-skip Phase7 RED). Race unsupported on this Windows cgo toolchain.
- Deferred Phase 4+: feature plugin/YAML, Call JSON traversal scanner, Aho-Corasick hot path, durable quarantine.

### Scratchpad — issue #151 Phase 4 (scanner + action plugin)

- Feature package `internal/plugins/features/secretsguard`: scan.go one-pass model-bound locations; guard block/redact/log; json_redact UseNumber + decoded-string match; config/plugin/bundle; no `internal/core` imports.
- Core matcher: Aho-Corasick (`aho_corasick.go`); `KnownPublicPrefix` registry + inventory attach; `NewStaticMatcherResolver` preserves prefixes.
- Scan limits: block/redact fail-closed (ignore audit best_effort); log `OutcomePass` + `ScanLimitHit`.
- Fuzz/bench: JSON redact invariants + guard scan; matcher no-match/many-pattern/many-match/large-tool-result (1 alloc/op no-match paths).
- Verified: gofmt; git diff --check; go build feature/core/cmd/lipstd; tests secretsguard(+feature)/extensions/runtime/runtimebundle/archtest (-skip FrontendMatrix|_RED$); fuzz 3s PASS. No commit.
- Superseded: feature is now registered in `standard_table` as `secrets-guard`; Phases 5–7 landed (quarantine, audit/metrics, frontend matrix).

### Scratchpad — issue #151 Phase 5 (quarantine)

- Memory/SQLite/Bun `Store.Quarantine` atomic+idempotent; audit `secret_guard`/`blocked` once; migration columns status/quarantine*; empty status = active.
- Manager BeginTurn → `ErrSessionQuarantined`; lipapi `SessionDeniedQuarantined` + lipapidenial map.
- Runtime: quarantine before client denial; persistence failure → storage denial + process fault; `AssertActive` immediately before backend Open; best-effort `ALegLifecycle.CancelALeg` after quarantine.
- Limitation: cancel only reaches A-legs already registered with `ALegLifecycle`; turns paused before lifecycle registration rely on pre-dispatch AssertActive.
- Verified: storecontract/memory/sqlite/bunstore/lipapidenial/app + runtime quarantine tests; build lipstd. No Phase 6. No commit.
### Scratchpad — 2026-07-16T19:24:53.106Z

- Completed final acceptance and independent security reviews for issue #151 secret guard. No actionable issue-151 defects remained.
- Verification passed: git diff --check, go build ./..., go vet ./..., go test ./..., internal/archtest, precommit runtime tags, SQLite integration tag, three 3-second fuzz targets, and matcher benchmark smoke tests.
- A single unrelated metering journalstore SQLITE_BUSY concurrency flake appeared once during a full-suite run; the isolated test passed 10 repetitions and the subsequent full suite passed. No unrelated code was changed.
- Residual environment gaps: Windows race testing is unsupported/not run; external Postgres-backed Bun integration was not run.

### Scratchpad — issue #151 review findings resolution

- Fail-closed when SecureSession present but SessionID empty on block; process-wide quarantine fault latched.
- Log+scan_max_bytes stays fail-open; louder audit/metrics + operator warning; acceptance test for secret past bound.
- D10: catalog enablement follows feature flag only (guards alone no longer load env catalog).
- Metrics: decision label `mixed` for multi-category; IncMatch once per distinct category.
- Tests: case-sensitivity negative, DecisionEvent field E2E, Evaluate-once under failover, snapshot guards/resolver contract.
- Docs: secrets-guard.md package map/scan limits/quarantine fault/RuntimeSnapshot; EchoesVault + structure.md drift fixed.

### Scratchpad — modernize / Decision.Validate unblock

- Aligned `Decision.Validate` with log scan-limit fail-open (`OutcomePass` + `ScanLimitHit` + scan_limit metadata).
- Enabled `validation_test.go`; unskipped malformed-decision runner test; fixed stub findings (Location/OccurrenceCount).
- Modernize clean on secrets-guard packages; focused tests + example `lipstd check-config` green.

### Scratchpad — issue #151 Phase 1 docs update

- Documented the reviewer-resolution wording for exact-match v1: ingress-only, no response/egress scanning, no transformed forms, and log mode can still allow model echo to appear in downstream capture because ingress remains unchanged.
- Clarified multi-user scope as current-request credential only with safe attribution identifiers, single enabled instance per deployment, snapshot-only single-user catalogs with restart-on-rotation, opaque matcher in middleware request context, and value-based public-prefix provenance.
- Added the JSON target note: redact string values normally, scan object keys and scalar lexical values, and fail closed on non-string tokens that cannot be rewritten safely.
### Scratchpad — 2026-07-16T21:17:21.496Z

- Completed reviewer-resolution orchestration for issue #151 across six phases.
- Clarified v1 scope/non-goals: current request credential only in multi-user, attribution IDs not scanned, ingress-only exact matching, transformed forms excluded, RemoteAddr caveat, startup catalog snapshot/restart on rotation, one enabled guard instance.
- Added strict Decision/Finding shape validation before evidence/audit/metrics and made log scan-limit outcomes explicit.
- Extended valid JSON scanning to object keys and scalar lexical tokens; redact-mode unsupported key/scalar matches now atomically block with safe `unsupported_json_token` metadata. Fixed a review-found duplicate JSON finding merge.
- Removed the opaque matcher from public AuthenticationResult; local successful auth now attaches it only to request context and middleware preserves it across provider chaining.
- Added factory-key-correct rejection of multiple enabled secrets-guard instances at feature/runtime composition boundaries.
- Renamed frontend RED matrix to normal conformance coverage and explicitly tests assistant tool-call arguments across OpenAI Responses, OpenAI Chat, Anthropic, and Gemini.
- Fresh verification passed: git diff check, build, vet, archtest, full go test, precommit runtime tags, SQLite integration tag, matcher/JSON fuzz, matcher benchmark. Windows race and external Postgres-backed tests remain environment gaps.
