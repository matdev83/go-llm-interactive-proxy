## Session — 2026-07-08T00:42:00.000Z

Completed the Final Architectural Review Closure Plan. All nine plan todos landed with no deferrals.

### Phase 4 — executor refactor (F-04)

- Extracted `buildRoutePlan` + `routePlanState` into `internal/core/runtime/executor_route_plan.go`.
- Extracted `openInitialAttempt` loop (tryPlanOpenOnce + B-leg register) into `internal/core/runtime/executor_open_loop.go`.
- Extracted `assembleExecutorStream` (retryRecvStream + interleaved wrappers) into `internal/core/runtime/executor_assemble_stream.go`.
- `executor.go` now delegates: prepareRequest -> buildRoutePlan -> openInitialAttempt -> assembleExecutorStream (~118 lines, down from ~380).
- Introduced grouped `ExecutorConfig` structs (`CoreRuntime`, `RoutingRuntime`, `SecurityRuntime`, `AccountingRuntime`, `ObservabilityRuntime`, `ExtensionRuntime`, `InterleavedRuntime`) and `NewExecutor(cfg)` in `executor_config.go`. Mutex fields kept on `Executor` only (vet-safe).
- Added `TestExecutor()` test wiring helper and `internal/testkit/executor_builder.go` (`NewTestExecutor` + options). Migrated ~76 test files from `&Executor{...}` literals.
- Tightened `executor.go` critical-file budget 380 -> 150 in `internal/archtest/critical_files.go`.
- Added `executor_characterization_matrix_test.go` mapping resolution-plan scenarios to existing tests.

### Hexagonal closure (F-08, F-02, F-09)

- `internal/featurebundle/merge_surface.go`: `MergedFeatureSurface` now uses SDK hook slices (`SubmitHooks`, `RequestPartHooks`, `ResponsePartHooks`, `ToolReactors`) instead of `hooks.Config`. `internal/core/hooks` import removed from `featurebundle`.
- `BuildFeatureHooks` + `hooks.New` moved to `internal/infra/runtimebundle/build_feature_hooks.go` (composition root).
- `testdata/architecture/hexagonal_migration_baseline.json`: `featurebundle`, `runtimebundle`, `internal/core/extensions` reclassified to `aligned`; backlogs `done`; `featurebundle` added to `retired_exceptions`.
- `internal/archtest/hexagonal_migration_baseline_test.go`: now requires zero `exception` entries (closure target).
- `internal/core/extensions/stage_panic_log.go`: `lineage` import retained — `execctx` has no `TraceID(ctx)`/`ALegID(ctx)` context helpers; baseline justification documents why it stays.

### Documentation (F-12)

- `docs/architecture.md`: composition path now references `standardplugins.InstallStandardBundleOn`, `featurebundle.MergeFeatureSurface`, `make arch-report`; added links to core-boundaries/enterprise-extension/feature-bridge/guardrails; added F-11 single-module note.
- `docs/architecture-guardrails.md`: `pluginreg/standard_table.go` -> `standardplugins/standard_table.go`; schema_version 2 -> 3; noted CI publishes arch-report artifact.
- `docs/feature-bridge-retirement-checklist.md`: merge surface noted as SDK-slice-only; `BuildFeatureHooks`/`hooks.New` now in runtimebundle.
- `.kiro/steering/structure.md`, `EchoesVault/pages/package-map.md`, `EchoesVault/pages/architecture-overview.md`, `EchoesVault/pages/plugin-system.md`, `EchoesVault/pages/product-overview.md`, `EchoesVault/pages/codex-app-server-backend.md`: stale `pluginreg` references corrected to `standardplugins`/`featurebundle`/`runtimebundle`.
- `README.md`: repository layout updated to list `internal/standardplugins/` and `internal/featurebundle/`.

### Verification (d-verify)

- `make test-unit`: PASS (all packages).
- `make quality-checks`: PASS (gofmt, modules, build, go vet, goroutine allowlist, regex hot-path, archtest guardrails).
- `make arch-report`: 11 aligned, 0 extract, 0 exception; `executor.go` at 118 lines.
- `lipstd check-config` / `routes` against `config/examples/dogfood-local-stub.yaml`: PASS (no API keys required).
- No client-visible behavior change (internal package relocation only; regression tests green). No DI containers or init registration introduced.

## Cleanup session — 2026-07-08 (post-closure follow-ups)

Architecture review cleanup follow-ups executed after the final closure PR (#116).

### PR 1 — Stale comment hygiene

- Fixed 9 stale `pluginreg.DefaultWireModel` → `standardplugins.DefaultWireModel` references across `options.go`, `build_executor.go`, `reg.go`, `built.go`, `route.go`, `effective_default_route.go`, `doc.go`.
- Fixed `BuildOptions.SecureSessionStore` → `BuildOptions.Diagnostics.SecureSessionStore` and `BuildOptions.HTTPClient` → `BuildOptions.Infra.HTTPClient` in comments.
- Updated `pluginreg` package doc to reflect current ownership (registry mechanism only, standard tables in `standardplugins`).

### PR 2 — Removed `runtimebundle.BuildExecutor`

- Deleted the legacy wrapper from `build.go`; removed unused `b2bua`/`pluginreg` imports.
- Migrated 2 tests to use `Build` directly (`TestBuild_productionClockAndRNG`, `TestBuild_defaultBackendFromEffectiveRoute`).
- Updated `critical_files.go` comment.

### PR 3 — Removed `pluginreg/standardbundle` façade

- Refined guardrail test `TestCompositionLayersDoNotRegisterStandardBundle` to exempt `bootstrap_plan.go` as the single composition-root startup path.
- Updated `bootstrap_plan.go` to call `standardplugins.InstallStandardBundleOn` directly.
- Deleted entire `internal/pluginreg/standardbundle/` package.
- Fixed stale `standardbundle` reference in `pluginreg/main_test.go`.

### PR 4 — Composition-root role metadata

- Added `role` field to `hexagonalBaselineEntry` and `baselineEntry` structs.
- Validation: `role` must be `""` or `"composition_root"`; `composition_root` only on `aligned` packages.
- Set `"role": "composition_root"` on `runtimebundle` in baseline JSON.
- Updated `arch-report.go` to display Role column.

### PR 5 — Removed `standardplugins/types.go` alias trampoline

- Deleted the migration shim; qualified ~15 files in `standardplugins/` with `pluginreg.X` references.
- Fixed comment readability in `backend_security_test.go` (prose uses unqualified names, code uses qualified).

### PR 6 — Simplified `MergeFeatureSurface`

- Replaced 4-pass count/grow/append/copy boilerplate with `MergeBundles()` + `(*MergedFeatureSurface).Append()` + `buildEnabledFeatureBundles()` helpers.
- Added 3 new direct tests in `merge_surface_test.go`.

### PR 7 — Executor construction hardening (deferred item from original review)

- Moved all post-construction field mutations from `buildExecutorRuntime` into `ExecutorConfig` grouped structs.
- `NewExecutor` is now a strong invariant boundary: all fields set at construction, no post-construction mutation.
- Token accounting, auth events, session audit policy, synthetic local principal, metrics, and model catalog wiring all pass through `ExecutorConfig`.
- Helper functions refactored: `applyInterleavedToExecutor` → `interleavedExecutorRuntime`, `applySecureSessionToExecutor` → `securityRuntimeFromSecureSession`, `wireModelCatalogExecutor` → `routingRuntimeFromModelCatalog`, `attachModelCatalog` returns `(RoutingRuntime, *CatalogRuntime)` instead of mutating.

### Documentation updates

- ADR 0001: updated to reflect `standardplugins.InstallStandardBundleOn`, deleted `standardbundle` façade reference, added cleanup update note.
- `architecture-guardrails.md`: updated scope caveats for `standardplugins.DefaultWireModel`, added `bootstrap_plan.go` exemption note, added `role` metadata to baseline description, updated automated checks table row.
- `architecture.md`: updated composition path to note `MergeBundles`/`Append` helpers.
- `feature-bridge-retirement-checklist.md`: noted `standardbundle` façade deletion and `MergeFeatureSurface` simplification.
- `internal/plugins/features/README.md`: corrected wiring reference from `pluginreg` to `standardplugins`.
- `EchoesVault/pages/plugin-system.md`: noted `MergeBundles`/`Append` simplification.
- Specs (`architecture-review.md`, `findings-register.md`, `source-index.md`, `resolution-plan.md`): updated `BuildExecutor` references to note deletion, corrected `standard_table.go` location, updated `featurebundle.go` to `merge_surface.go`, added completion status to resolution-plan tasks.

### Verification (cleanup session)

- `go build ./...`: PASS
- `make quality-checks`: PASS (all 7 steps)
- `make arch-report`: PASS (Role column displayed, composition_root on runtimebundle)
- Code review: no remaining stale references to deleted packages/symbols; no behavior changes in Executor hardening.

## Final follow-up session — 2026-07-08 (annotations, invariant guard, smoke test, full QA)

### Resolution-plan annotation

- Annotated all Phase 0–7 tasks in `specs/go_llm_proxy_arch_review/resolution-plan.md` with `> **Status: Completed.**` blockquotes and strikethrough on acceptance criteria.
- All five PR groups in the implementation order section marked as completed.
- Final acceptance definition uses ✅ checkmarks with closing statement: **All acceptance criteria met. The de-slopification effort is complete.**

### Executor construction invariant guard test

- Added `internal/archtest/executor_invariant_test.go` with `TestExecutorConstructionNoPostConstructionMutation`.
- Uses AST inspection to walk non-test `.go` files in `internal/infra/runtimebundle/` and reject any assignment to executor fields after `runtime.NewExecutor` is called.
- Detects two patterns: direct mutation (`exec.Field = ...`) and struct-field mutation (`rt.Exec.Field = ...`).
- Enforces that `NewExecutor` remains a strong invariant boundary — all fields must pass through `ExecutorConfig` at construction time.
- Code reviewed: false-positive risk is low (scoped to runtimebundle package); false-negative risk acceptable (direct match covers the actual code pattern).

### End-to-end smoke test

- Built `lipstd` binary from the refactored codebase.
- Created `config/smoke-nvidia.yaml` enabling the `nvidia` backend with model `stepfun-ai/step-3.7-flash`.
- Started server on `127.0.0.1:18090`; healthz responded `{"status":"ok"}`.
- **Non-streaming:** `POST /v1/chat/completions` → NVIDIA `stepfun-ai/step-3.7-flash` → real response: *"Hey there, friend!"*
- **Streaming:** `POST /v1/chat/completions` with `stream:true` → SSE deltas with reasoning + content chunks, terminated with `data: [DONE]`.
- Server logs: no errors; auth decisions, route resolution, and session lifecycle all clean.
- Temporary config and binary cleaned up after testing.

### Documentation updates (final session)

- `EchoesVault/pages/testing-strategy.md`: added Architecture Guardrail Tests section listing all archtest invariants including the new Executor construction invariant.

### Verification (final session)

- `make test-unit`: PASS (35 packages, 0 failures).
- `make quality-checks`: PASS (all 7 steps: gofmt, modules, build, vet, goroutine allowlist, regex hot-path, archtest guardrails).
- `go vet ./internal/archtest/...`: clean.
- Executor invariant guard test: PASS.
- End-to-end smoke test: PASS (non-streaming + streaming against real NVIDIA backend).
