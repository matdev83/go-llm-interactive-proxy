# 2026-07-21

## Runtime reload Phase 6.5 docs

- Added operator guide `docs/runtime-config-reload.md` and ADR `docs/adr/0008-versioned-runtime-config-reload.md`.
- Linked from README, architecture, runtime-flow, architecture-guardrails, release-gates, spec-bundle-index, dogfood-local, ADR 0006.
- EchoesVault: new `runtime-config-reload` page + index entry; architecture-overview and security-auth cross-links.
- Corrected Kiro Implementation Notes (text only): management is `LIP_RELOAD_MANAGEMENT_ADDRESS` opt-in; `Runtime.Close` is serialized/retryable (not `sync.Once`). Task checkboxes unchanged.
