# 2026-07-18

### Scratchpad - Cursor SDK Task 6.1

- Composed Go-LIP integration repaired after review REJECT: direct `HistoryMarker{}`+`LiveCount==0`+create-count bootstrap proof in `cursorsdk` with real runtime+Executor; ACP stub explicit route via frontend/executor; non-vacuous pre-output `bridge_exited` on Recv + failover-when-planned; `ActionWaitForFile` replaces sleep.
- GREEN: four validation packages + cursorsdk/fakebridge; vet; linux/windows `-c`. Race skipped (Windows cgo). No commit.

### Scratchpad - Cursor SDK Task 6.2

- Lifecycle contract for `RunStream`; archtest `@cursor/sdk` only under `bridge/` + core no cursorsdk import; goleak concurrency/cancel/restart/idle/shutdown; runtimebundle cursorsdk Close on partial rollback; `FuzzDecodeLine`/`FuzzMapBridgeEvent` + Makefile wiring.
- GREEN: cursorsdk/... + archtest + runtimebundle; fuzz smoke; vet; cross `-c`. `-race` blocked Windows cgo. No commit.

### Scratchpad - Cursor SDK Task 6.2 repair

- Split kitchen-sink concurrency into deterministic phases with explicit asserts (all PrepareSend errors, idle reap LiveCount0/Marker/dispose1, CancelInvalidate once, gen restart dispose1+marker gen2, Close idempotent dispose N). Rollback wrapped Close counter=1 + second call no increment.
- GREEN: focused Concurrency_* + rollback; full cursorsdk/archtest/runtimebundle; fuzz smoke; vet; cross `-c`. `-race` blocked Windows cgo. No commit.

### Scratchpad - Cursor SDK Task 6.2 rollback fix

- Removed self-fulfilling CAS; count every wrapper/inner Close; rollback=1; direct second Close ok counts=2.
- GREEN: focused rollback + Concurrency_*; full packages; fuzz; vet; cross `-c`. No commit.

### Scratchpad - Cursor SDK Task 5.2

- Assembled operational experimental `cursorsdk` backend: Scaffold → `backendRuntime` with bridge, live `models/list` inventory, SessionPool, FailureCoordinator, Open→PrepareSend/CommitSend→RunStream, Close pool-then-bridge.
- Session identity: authoritative session when present; otherwise attempt-scoped (`attempt:<callID>` / random) — client hints alone do not merge.
- Bridge fix: auto-arm run subscription on `agent/send` response; `SubscribeRun` reclaims armed sub (prevents event drop / sequence_regression race).
- Removed phase-scaffold `ErrConstructionNotImplemented` expectation from standardplugins; max-output/tools/structured remain fail-closed; reasoning exactness preserved.
- GREEN: focused + full cursorsdk; modelregistry; standardplugins Cursor/bundle filters; gofmt/vet; linux/windows cross-compile. Race skipped (Windows cgo). No commit.

### Scratchpad - Cursor SDK Task 5.2 repair

- Review REJECT: runID reuse/stale auto-arm. Added `claimed`/`sendBound`/`closed` accessors; preserve pre-subscribe; replace closed; `errRunIDConflict` on overlap; SubscribeRun buffered-closed reclaim; overflow deterministic; fakebridge unique IDs; removed `lastOpenKey`; `closePoolThenBridge` dual-error test.
- GREEN: lifecycle + Open/Inventory/Reasoning/Close; full cursorsdk; fakebridge; standardplugins/modelregistry; vet; cross-compile. Race skipped. No commit.

### Scratchpad - Cursor SDK Task 5.3

- Enforced MCP normalize (order-independent ≤256KiB), empty/exact setting sources, sandbox required fail-closed via bridge `sandboxSupported`, explicit `off` incl. Windows, env allowlist, no customTools/local.force/retries, AgentKey invalidation on security config.
- GREEN: focused `MCP|Setting|Sandbox|Environment|Security`; full cursorsdk/fakebridge/protocol/standardplugins; vet; cross-compile. Parent 5 open (5.4 pending). No commit.

### Scratchpad - Cursor SDK Task 5.3 repair

- Review REJECT: empty SandboxMode≡required; wire fakebridge sandboxSupported/omit tests via injectEnvStarter (no override, no prod allowlist FAKE_BRIDGE_SCRIPT); Node detectPlatform injectable win32/linux asserts.
- GREEN: focused+full Go; vet; cross; Node 67/67. No commit.

### Scratchpad - Cursor SDK Task 5.4

- Safe `Diag`/`StatusSnapshot`: allowlisted slog fields; pool/bridge/run/discovery/shutdown emits; correlation via corediag; no SDK IDs/secrets/prompts.
- GREEN: focused `Diag|Log|Redact|Secret|Status`; full cursorsdk stack; vet; cross. Parent 5 marked complete. No commit.

### Scratchpad - Cursor SDK Task 5.4 repair

- `runDiagOutcome` once-state; record under lock, pool/slog after unlock; create/send `ClassifyFailure` + classified pool/run emits; terminal KindError post-output; hashed correlation/instance; typed secret/path/prompt redaction.
- GREEN: focused Diag|Log|Redact|Secret|Status; full cursorsdk+fakebridge+protocol+standardplugins; vet; linux/windows `-c`. Race skipped (Windows cgo). No commit.

### Scratchpad - Cursor SDK Task 6.3

- Opt-in live Node scenarios (`CURSOR_SDK_LIVE=1` + key); `mkWorkspace` sync|Promise awaited via `resolveWorkspace` + cleanup regression test; Go `LiveOptInReady` skip gate; fake-bridge `RunPlatformSmoke` (start/stream/cancel/crash/restart/rebootstrap/shutdown); native probe ready on this host; Make + workflow matrix.
- GREEN: `make test-cursor-sdk-live` → BLOCKED (no key); `make test-cursor-sdk-platform` ok; bridge typecheck/test/build/pack 74/74; Go LiveOptIn/PlatformSmoke/ProbeNative ready; vet; workflow YAML OK. BLOCKED: live network (key never used); cross-OS CI matrix not run here. No commit.

### Scratchpad - Cursor SDK Task 6.4

- Operator docs `docs/cursor-sdk-backend.md`; example `config/examples/cursor-sdk-experimental.yaml`; commented block in `config/config.yaml`; README + EchoesVault updates; bridge README link.
- Unblocked quality gate: `poolSmokeConfig` inlined (no test-only `testConfig`); allowlisted `cursorsdk/bridge_process.go` goroutines.
- GREEN: example BootstrapInspect + `lipstd check-config`/`routes`; `make quality-checks`; parent task 6 marked complete. No commit.

### Scratchpad - Cursor SDK Task 7.1

- Validation matrix: bridge typecheck/test/build/pack; targeted Go; quality-checks; make test; fuzz; lint+vuln; qa (Postgres DSN unset).
- Repairs: cursorsdk lint cleanup + `.golangci.yml` paralleltest exclusions for bridge lifecycle tests.
- BLOCKED/SKIPPED: Windows `npm ci` EPERM esbuild; `make test-race` / `-race` cgo; live scenarios (no key); host Postgres integration when DSN set (unrelated stale schema).
- Diff audit: no default-route switch, ACP removal, Cloud/resume/custom tools, or `pkg/lipapi`/`pkg/lipsdk` changes. No commit.

### Scratchpad - Cursor SDK Task 7.2

- ACP vs SDK comparison matrix package + `make test-cursor-sdk-comparison-report`; methodology doc; synthetic/blocked offline report; redaction/schema/experimental-non-default tests.
- Review repair: no synthetic comparative metrics; blocked/synthetic omit rate/count/latency; MD `-` for samples0; enum notes/blocked_reasons; stronger path scan; BuildReport scans hand-built docs; size bounds. Parent 7 unchecked pending ACCEPT. No commit.

### Durable - Cursor SDK Task 7 ACCEPT

- Parent Task 7 `[x]` (7.1–7.2 `[x]`). Final ACCEPT 2026-07-18 post repair-wave independent review: experimental rollout evidence done; measured live/comparative dogfood blocked residual (opt-in). Parent 2 also `[x]` (children already done).
- EchoesVault `cursor-sdk-backend` status: complete with blocked live residual. No commit.

### Scratchpad - Live/platform evidence honesty

- Removed `cursorsdk_live` fake-bridge Go smoke; live lane = Node `live-scenarios` only. Platform cancel asserts single cancelled terminal (`hold_until_cancel`). Live summary `complete|blocked|failed` with `ok` only when complete.
- GREEN: fakebridge HoldUntilCancel; PlatformSmoke/Probe/LiveOptIn; bridge typecheck/test 81/build; `make test-cursor-sdk-platform`; `make test-cursor-sdk-live` BLOCKED exit 0 (no key).

### Scratchpad - Repair-wave bookkeeping

- Tasks 3.3/4.1/4.3/5.3/6.3/7.1 evidence notes; parents 2+7 `[x]`; overclaims removed (sandbox OS heuristic, pending ACCEPT).
- GREEN: full `cursorsdk/...`; Node 81+typecheck; platform; live BLOCKED; comparison measured=0/synthetic=4/blocked=20; golangci cursorsdk 0. No production edit. No commit.
