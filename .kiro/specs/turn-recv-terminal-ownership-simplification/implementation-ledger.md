# Implementation Ledger

This ledger records implementation commits and direct verification evidence. Overall spec completion remains false until the implementation is merged and verified on `main`.

| Task | Status | Commit | Verification evidence |
|---|---|---|---|
| 1.1 Ownership/coupling baseline | Complete | `2bb1c4c2` | Deterministic AST baseline: 89 fields, 12 synchronization primitives, 91 receiver methods, 34 domain-package fan-out, 39 Executor-reachable methods, and 62 state-copy assignments. Opt-in final-topology gate is RED for the intended flattened-ownership findings. |
| 1.2 Recv/Close/failure/terminal characterization | Complete | `2bb1c4c2` | Exact event/error/terminal ordering, blocked Recv versus Close, cancel/timeout/EOF/panic behavior, loser claims, and detached bounded cleanup characterized; 100 repeated scheduling runs passed. |
| 1.3 Response/usage/billing/security/observer characterization | Complete | `2bb1c4c2` | Existing runtime characterization matrix plus new sequential-replacement evidence test pins sideband usage, two correctly attributed B-leg records, one call closure, observer output, and ordering; uncached runtime package passed. |
| 1.4 Replacement pinning/interleaved characterization | Complete | `2bb1c4c2` | Existing interleaved/reload suite plus new continuation test pins exec/session/metering/authority/security/routing/model/billing facts across refresh; uncached and 100 repeated focused runs passed. |
| 2.1 Immutable receive-turn facts | Complete | `4528016b` | `recvTurnFacts` is the sole construction-time authority for cloned request, pinned context/model views, route/security identity, and stable economic owner references. Focused facts/ratchet tests, uncached runtime, runtime vet, and 100 repeated Recv/Close/domain/pinning runs passed. The opt-in target improved from 89 to 67 direct façade fields and from 62 to 40 state-copy assignments; remaining target debt is assigned to later owner tasks. |
| 2.2 Current-B-leg attempt owner | Complete | `296205ce` | One private `attemptSession` now owns the managed stream, B-leg/candidate identity, authority lifecycle, and fresh ScopeAttempt terminal behind a short-lock `attemptSlot`. Direct façade attempt fields/reset plumbing and transitional forwarding methods were deleted. Uncached runtime, runtime vet, focused repeated Recv/Close/replacement/domain tests, and diff checks passed; the opt-in target improved to 62 fields, 11 direct sync primitives, 86 receiver methods, and 35 state-copy assignments. |
| 2.3 Attempt-local accounting/tool/prompt-cache state | Complete | `362f13b1` | `attemptSession` now owns provider usage accounting/dedupe, the per-B-leg tool assembler, backend-derived prompt-cache source/controller, and final stream-observation session. A production-path replacement regression proves old state is finalized/discarded and replacement state derives from the new stream/backend; 20 repeated runs, uncached runtime, runtime vet, formatting, and diff checks passed. The opt-in target improved to 55 direct fields and 29 state-copy assignments. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 2.4 Coherent current-attempt snapshot/swap | Complete | `ddc02f2d` | `attemptSlot` now atomically closes replacement publication against `Close` and conditionally swaps complete attempts without holding locks across Open, terminal, authority, billing, or stream cleanup effects. Sideband usage stays bound to its source attempt, and rejected fresh attempts use their own terminal/authority for detached cleanup. Deterministic Close/open, stale-snapshot, and sideband-retirement tests plus relevant existing lifecycle tests passed 50 repeated runs; uncached runtime, runtime vet, formatting, and diff checks passed. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 3.1 TurnTerminal and commitment authority | Complete | `f1c90f54` | Package-private `turnTerminal` now owns the sole request commitment, completion publication, and request-scope terminal while composing with an explicitly supplied `AttemptSession` terminal. Exact tests cover idempotent commitment, attempt replacement lifetime, request/attempt effects and conflicts, concurrent published outcomes, committed gate rejection, and finished publication. New and existing terminal/Recv-Close/gate/interleaved matrices passed 50 repeated runs; uncached runtime, runtime vet, formatting, and diff checks passed. The opt-in target improved to 52 direct fields, 7 direct sync primitives, and removed the forbidden committed/finished/requestTerm fields. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 3.2 Request authority/metering/billing closure | Complete | `64fed62a` | `turnTerminal` now owns request-lifetime accounting-finalization and BillingCall closure synchronization; each `attemptSession` owns its B-leg record dedupe. Terminal, cancellation, partial, usage, authority, and metering paths use one explicit captured attempt, including after replacement. Controlled RED proved the former current-slot read misattributed old-attempt backend egress to the replacement B-leg; focused Task 3.2 and combined billing/authority/terminal/Recv-Close/replacement tests passed 50 repeated runs, with uncached runtime, runtime vet, formatting, and diff checks green. Full `go test ./...` reached only the intentionally RED Phase 5 architecture line budget (81,511 measured versus 80,936 target). Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 3.3 Explicit A-leg/interleaved ownership | Complete | `12807c56` | `turnTerminal` now contains the explicit base-versus-outer A-leg end mode and one concrete end-once/lifecycle authority. Hidden and visible thinker/executor continuations share that authority while retaining their existing stage terminal behavior. The façade's `aScope`, `holdALegEnd`, and `endOnce` fields and its end forwarding helper were deleted. Focused ownership tests and the combined interleaved/A-leg/Recv-Close/cancellation matrix passed 50 repeated runs; uncached runtime, runtime vet, formatting, and diff checks passed. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 3.4 Consolidated terminal entry points | Complete | `2b350d4d` | `turnTerminal.terminalizeSnapshot` and `attemptSession.terminalizeSnapshot` are now the production terminal entry points over an explicitly captured attempt and immutable evidence snapshot. The five `retryRecvStream` terminal/billing forwarding surfaces and mutable owner snapshot helper were deleted. Focused terminal, billing, authority, Recv/Close, gate-replacement, and replacement tests passed 50 repeated runs; uncached runtime, runtime vet, formatting, and diff checks passed. The branch remains at 82 unique changed paths versus the merge base. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 4.1 Recovery controller/opener adapter | Complete | `d276884c` | One private `recoveryController` now owns selector/session state, exclusions, retry and TTFT budgets, request-size and affinity state, rejection/admission/context-limit/transform/parallel diagnostics, recovery policy, and interleaved cycle/suppression state. The D10 request/result adapter is the only recv/continuation component that translates to `attemptOpenParams`; it rejects committed turns and unretired prior attempts before opening. A real thinker-to-executor continuation regression proves the shared controller resets its per-attempt recovery policy. Focused and combined Task 4.1/replacement/failover/TTFT/idle/affinity/interleaved/secure-session tests passed 50 repeated runs; uncached runtime, runtime vet, formatting, and diff checks passed. The expected-red final topology improved from 44 to 25 direct façade fields and from 25 to 12 assembly/replacement state-copy assignments; remaining debt is response state and broad Executor removal. The branch remains at 84 unique changed paths. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 4.2 Replacement precedence/retirement | Complete | `1a84c635` | A production-path table pins exact no-eligible precedence as transport rejection, admission failure, capability rejection, context-limit exhaustion, transform exclusion, parallel failure, then wrapped routing exhaustion. Counting-opener tests prove committed turns and unretired prior attempts cannot open a replacement, while a retired attempt can; mandatory-recorder post-commit failure remains non-recoverable. A controlled RED that disabled transport precedence returned the admission error, proving the assertion is discriminating. Focused Task 4.2 tests, a combined replacement/failover/affinity/TTFT/interleaved matrix at 50 repetitions, uncached runtime, runtime vet, formatting, and diff checks passed. No production change was necessary because Task 4.1's recovery boundary and existing release ordering already satisfy the accepted behavior. The branch is at 85 unique changed paths versus the merge base. Windows race execution remains unavailable because ThreadSanitizer fails allocation with error 87. |
| 4.3 Response event/evidence pipeline | Complete | `561c4f09` | One private `responsePipeline` now owns customer-visible accumulation, internal/seen response evidence, completion-gate and recovery drains, distinct operator/customer usage evidence, and coherent terminal snapshots. Production code no longer directly accesses migrated fields, the response lock is never held across terminal-owner reads, and the pipeline cannot invoke terminal mutation; controlled RED tests caught and removed both a cross-owner snapshot deadlock and a duplicate response-level usage-dedupe map. Provider-key dedupe remains correctly attempt-local on `attemptSession`. Focused tests, 100 repeated response/Recv-Close/sideband/replacement/gate/recovery/TTFT/interleaved runs, uncached runtime, checkptr, runtime vet, formatting, and diff checks passed. The expected-red final topology improved to 15 direct façade fields, 65 receiver methods, 63 cross-domain methods, 36 Executor-reachable methods, and 9 assembly/replacement state-copy assignments; the remaining broad Executor/facade debt is assigned to Tasks 4.4 and 5.1. The branch is at 88 unique changed paths versus the merge base. Windows race execution remains unavailable because ThreadSanitizer/cgo cannot execute. |
| 4.4 Tool/security/logical observations | Complete | `ed5db5da` | `responsePipeline` now owns logical tool classification/committed-tool evidence, typed secure-recording outcome, compaction correlation, response observation ordering, traffic/usage emission, and once-only keep-warm handoff. Attempt-local tool assembly, prompt-cache source/controller, and final-stream observer sessions remain on `attemptSession`; obsolete recording/traffic/usage/observer façade fields and forwarding methods were deleted. Controlled RED architecture assertions caught a duplicate stored recorder error and the remaining forwarding-only BTP traffic receiver before their removal. Task 4.4 tests, uncached runtime, checkptr, runtime vet, build, formatting, and diff checks passed; the response/tool/security/compaction/replacement matrix passed 100 repetitions. The Close-while-blocked test passed in 25 isolated processes; `-count` parallel repetition intermittently returns `runtime: recovery turn already committed` on both unchanged `561c4f09` and this commit, so it is recorded as pre-existing parallel-test interaction rather than claimed green combined evidence. The expected-red final topology improved to 10 direct façade fields, 45 receiver methods, 43 cross-domain methods, 25 direct domain-package fan-out, and 30 Executor-reachable methods. Remaining façade and broad-Executor debt is assigned to Task 5.1. The branch is at 90 unique changed paths versus the merge base. Windows ThreadSanitizer remains unavailable. |
| 5.1 Small EventStream facade | Complete | `972f9258` | `retryRecvStream` now contains exactly five named owner references/values (`recvTurnFacts`, `responsePipeline`, `attemptSlot`, `turnTerminal`, `recoveryController`) with no broad `*Executor`, context cache, anonymous owner embedding, lazy owner construction, or owner-forwarding accessors. Runtime composition projects narrow terminal/response/recovery operations once, preserves the D10 opener as the sole upstream bridge, and retains nil-logger request/billing fact projection plus exact billing append diagnostic policy. Independent evidence: Task 5.1 structural/billing tests, focused authority tests, full uncached runtime, checkptr, vet, `git diff --check`, 50-run replacement/terminal/sideband scheduling, and 25/25 isolated blocked-Recv/Close processes passed. The opt-in final topology gate now reports only Task 5.2 debt: 34 receiver methods, 32 cross-domain methods, and fan-out 24. |
| 5.2 Ownership/dependency/deletion ratchets | Complete | `c46fa82f` | The always-on final topology gate now proves the façade has exactly five named lifetime owners, only `Recv`/`Close`, no broad `*Executor`, no direct synchronization, no cross-domain receiver helpers, no Executor-reachable methods, and no duplicated terminal/recovery/response state. The legacy `executor_recv_error.go` and `executor_recv_handlers.go` forwarding surfaces were deleted; replacement registration/publication remains Recv-owned while recovery returns a typed result. Stale observer/billing scanners now inspect the actual owners without weakening hook/gate/final-observer ordering. A deterministic RED Close-publication-midpoint test exposed `runtime: recovery turn already committed`; the narrow attempt-slot publication query makes that state return EOF/A-leg cancellation and the original blocked-Recv/Close test passed 50 repetitions. Full uncached runtime and archtest packages, checkptr, runtime/archtest vet, formatting, and diff checks passed. The accepted explicit-owner/evidence boundaries add 1,581 non-test `internal/core` lines versus the 80,936 merge-base measurement (37 in the final deletion wave), documented under the standard measured-plus-25 ratchet; structural responsibility and authority handoffs, rather than LOC alone, are the accepted invariant reduction. The branch is at 95 unique changed paths versus the merge base, below the authorized 200 ceiling. Windows ThreadSanitizer remains unavailable. |
| 5.3 Repository regression gates | Complete with repository-baseline QA limitation | `fdabd831` | Final clean-HEAD runs passed `go mod verify`, affected-package vet, formatting/diff checks, `make quality-checks`, `make test-unit`, `make test`, `make parity-checks`, QA's test stage, full uncached runtime/archtest, runtime checkptr, and `golangci-lint run --new-from-rev d6065717...` with zero findings. Attempt swap/publication, blocked Recv/Close including the deterministic publication midpoint, terminal claims, response snapshots, billing closure, sideband retirement, secure-recorder failure, and leak/deadlock cases passed at 50 repetitions. Windows ThreadSanitizer cannot allocate and exits with error 87, so no race evidence is claimed. `make qa` remains red only at its repo-wide PATH-provided golangci-lint step (141 findings); the clean merge-base independently fails the same step with 143 findings, while all nine initially identified branch findings and all progressively exposed diff findings were removed. No unrelated baseline lint cleanup or suppression was introduced. |
| 5.4 Final simplification review/handoff | Complete | `d39ec8fb`, `fdabd831` | GO on the ownership graph: the façade is exactly five named owners with only `Recv`/`Close`; the old recv error/handler files and forwarding-only recovery helpers are deleted; current attempt, attempt terminal, commitment, recovery diagnostics, response accumulation, and request terminal each have one authority. CodeGraph confirms the sole broad `*Executor` reach is the private D10 `newReplacementOpener`, which translates typed immutable facts/recovery/prior-attempt input to the unchanged upstream `attemptOpenParams`; recovery returns a typed open/attempt result while Recv retains B-leg registration and slot publication. No universal turn bag, services aggregate, DI/container/resolver framework, public SDK/config/plugin/routing change, or tranche-2 pipeline redesign was introduced. Final `internal/core` measures 82,476 non-test lines (+1,540 versus the 80,936 merge base, but -4 in the final deletion/cleanup wave) under a tightened measured-plus-25 ratchet; the accepted growth is the documented cost of explicit lifetime owners/evidence seams while façade responsibilities and implicit handoffs collapse. The branch remains below the authorized 200-path ceiling. Overall spec completion/archive intentionally remains false until merge and verification on `main`; repo-wide QA lint is the only local gate limitation. |

## Preflight

- Worktree: `C:\Users\Mateusz\source\repos\go-llm-interactive-proxy-turn-recv-terminal-ownership-simplification`
- Branch: `feat/turn-recv-terminal-ownership-simplification`
- Initial HEAD and `origin/main`: `d606571735701ad6f767bd35b939597b4f8ad44f`
- Initial state: clean; branch ahead/behind `origin/main`: `0/0`; no rebase required.
- Accepted requirements, design, tasks, brownfield analysis, and validation were rechecked against the current indexed runtime ownership/call graph before approval.
