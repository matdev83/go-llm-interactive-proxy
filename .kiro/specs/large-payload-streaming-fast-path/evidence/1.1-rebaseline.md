# Task 1.1 — Implementation-Start Rebaseline Evidence

Task: 1.1 Record implementation-start SHA and compare it with this revision
(Requirements: 22, especially 22.8. Non-behavioral research/evidence task: no production changes.)

## 1. SHA record

- Implementation-start HEAD: `3da34d7875443355d65cb9d7df649555dfad3edb`
  (`git rev-parse HEAD`, branch `feat/large-payload-streaming-fast-path`, worktree clean at start.)
- Spec revision baseline (`spec.json reviewed_against_main`): `b08c60846a2a0119eeefef135cd6bbe06162a894`
- Delta `b08c608..3da34d78`: 54 files changed. **Zero** changed files under
  `internal/plugins/frontends/`, `internal/core/`, `internal/infra/runtimebundle/`,
  `internal/infra/runtimehost/`, `internal/archtest/`, `internal/stdhttp/`,
  `pkg/lipapi/`, `pkg/lipsdk/`, `pkg/lipruntime/`
  (`git diff --stat <base> HEAD -- <those paths>` is empty).
- Only `*.go` changes in delta: `connectors/*` (independent optional-connector
  modules), `connector-support/oauthcred/*` (connector support package),
  `internal/standardplugins/provider_profile_binding.go` (+tightened
  custom-compatible marker validation, composition-time errors only),
  `internal/standardplugins/provider_profile_binding_test.go`,
  `internal/qa/database_parity_docs_policy_test.go`. Remainder is
  steering/spec/docs prose. None of these touch the section-2 seam list.

## 2. Seam re-check (HEAD working tree, CodeGraph + source)

All references below verified against current on-disk source at implementation-start HEAD.

### Frontend ingress / decode / admission (design 1, 2; req 1, 2, 3, 6)

- Shared order in `internal/plugins/frontends/frontendpipe/pipe.go`:
  `ServeHTTP` (207) → keepalive-interval ctx (209-212) → method (213) →
  `AltServe` (217) → `MatchPath` (220) → `reqbody.ReadAll` under
  `maxBodyLimit()` (226-235) → executor-nil check (240) → header selector (245)
  → optional legacy `ResolveRouteSelector(r, body, pm)` (246-250) →
  `jsonguard.PreflightWithContext` (251) → single `decodeqos.TryAdmit` with
  `int64(len(body))` (259) → `decodeqos.Guard` holding the permit across
  `RouteFromBodyModel` defaulting + `Spec.Decode` (268-284) → `call.Validate` /
  `AfterDecode` / traffic emit (292-316) → `spec.execute` (326) → `WrapStream`
  (338) → `BuildEncodeOpts` + `WriteStream`/`WriteNonStream` (358-366).
  Matches design section 1 oracle exactly.
- `reqbody.ReadAll` (`internal/plugins/frontends/reqbody/body.go:21`): still
  `http.MaxBytesReader` + transparent gzip with limit on decompressed bytes.
- `decodeqos.TryAdmit` (`internal/plugins/frontends/decodeqos/admission_http.go:24`):
  nil-acquirer means unlimited; `Guard` (33) releases exactly once incl. panic.
- `RouteFromBodyModel=true`, no legacy resolver: `openairesponses/handler.go:84`,
  `openailegacy/handler.go:64`, `anthropic/handler.go:66`. Only `gemini/handler.go:61`
  sets `ResolveRouteSelector` — assumption holds for Lanes 1-3.
- OpenResponses outer ordering preserved: `openresponses/handler.go:84` comment
  ("Auth and JSON Content-Type stay on the handler") + media-type check (94-100)
  runs before `frontendpipe.ServeHTTP` (100).

### Generated planes (design 7; req 5)

- `pkg/lipsdk/feature/plane_manifest.go`: `StandardPlanes` (1001) is the sole
  plane census; `FrozenPlaneSet` (`pkg/lipsdk/feature/frozen.go:10`) is the frozen set.
- `PlaneLocalTurnHandlers` (931), `PlaneSecretGuardExecution` (per StandardPlanes
  refs), `PlaneTerminalDecisionProvider` (975) all present.
- `pkg/lipsdk/feature/plane_manifest_test.go:22` pins exactly **26** standard planes.
  No second plane registry observed.

### Request-generation binding (req 1.9, 5.11, 6.7)

- `internal/infra/runtimehost/generation_dispatcher.go:25-49`: each request acquires
  exactly one generation lease, binds `newRequestBinding(lease)` +
  `BindModelViewsIfPresent`, delegates, releases exactly once.
- `internal/infra/runtimehost/generation_executor.go:36`: stable delegating
  `GenerationExecutor` (`ExecutorView`) backed by the manager. No cross-generation
  execution path observed.

### Secure-session lifecycle (design 10; req 14)

- `SecureSession.BeginTurn` gate: `internal/core/securesession/app/manager.go:38`
  with `BeginInput`/`BeginResult` (`manager_types.go:38/54`); resume-vs-new split,
  sensitive carrier (`ResponseMetadata`, raw resume token create-only).
- No `BeginTurn` inside frontend ingress/decode/admission path (pipe.go 207-316
  contains none).

### Metering / accounting (design 10; req 15)

- Call-cloning checkpoint helpers still the current API:
  `internal/core/metering/checkpoint/capture.go` (`CaptureFrontendIngress`,
  `CaptureBackendIngress`, `BindScope`, `MergeFrontendIngressQuantities`,
  `MergeBackendIngressQuantities`); `pkg/lipsdk/metering/checkpoint.go:15`
  (`Checkpoint`). Spec assumption "wire path shall not call Call-cloning helpers"
  remains meaningful — no wire-native checkpoint exists yet.
- Billing two-seam rule intact in code: cheap credit screen + atomic exposure
  admission via `internal/infra/runtimebundle/billing_compose.go:31`
  (`ComposeBilling` requires store/catalog/currency/max-output/terminal sink,
  exposure + credit store interfaces); `BillingIdentity` Call-shaped surface still
  present (`billing_compose.go`, `billing_authoritative_gate_test.go`, etc.),
  so the default-blocker assumption holds.

### Routing / route override (design 8, 12; req 7)

- `RouteOverrideReader routeoverride.Reader` still a narrow executor port
  (`internal/core/runtime/executor_config.go:175`), wired by composition
  (`internal/infra/runtimebundle/build_executor.go:231`). "Presence is not a
  blocker" assumption holds structurally.

### Backend contracts (design 9; req 8, 12)

- `execbackend.Backend` remains the internal provider-neutral backend value
  (`internal/standardplugins/provider_profile_binding.go:237+` builders return it).
- Grep for `OpenWire|ResolveWireRequest|ResolveWireDomain|AssessLargeBody|ExecuteLargeBody`
  across `internal/ pkg/ cmd/` returns **zero** hits: clean pre-production baseline,
  no partial fast-path implementation to reconcile.

### Keepalive / response ownership (design 13; req 18)

- `StreamKeepaliveInterval` → `stream.ContextWithKeepaliveInterval` before body
  read (`pipe.go:209-212`; `internal/core/stream/recovery_keepalive.go:17`).
- Streaming `Execute` wrapped in `holdalive.Wait` with `PreRequestKeepalive`
  (`pipe.go:194-204`; `internal/plugins/frontends/holdalive/wait.go:36`).
- Response ownership stays frontend-side: `Spec` carries `AfterDecode`,
  `WrapStream`, `BuildEncodeOpts`, `WriteStream`, `WriteNonStream`
  (`pipe.go:135-141`). No fake-Call construction observed.

## 3. Drift findings

### D1 (gate-red at start, steering prose — NOT a code-seam difference)

Between spec baseline and implementation start, commit `1aa52b22`
("docs(steering): refocus project memory on durable principles (#617)") removed the
billing-exposure markers that `internal/archtest/billing_exposure_docs_contract_test.go`
(`TestBillingExposureDocsRejectRetiredHoldAuthorizeInvariant`, unchanged in delta)
requires:

- `.kiro/steering/product.md`: missing `cheap credit screen`, `atomic operational
  exposure`, `BillingCallID` (all counts 0 at HEAD; `cheap credit screen` count was 1
  at `b08c608`).
- `.kiro/steering/structure.md`: missing `BillingCallID`, `operational exposure`,
  `post-usage` (all counts 0 at HEAD).

Consequence: the canonical CI architecture gate is RED at implementation start
(see section 4), for a reason unrelated to any 1.1 seam assumption. The production
billing *code* seams (section 2, metering/accounting) are unchanged, so this does
not invalidate the spec's technical assumptions — but it must be remediated before
production workstreams claim green gates.

Exact remediation (parent decision, outside this task's no-production-change scope):

- Option A (preferred): restore the three markers to `.kiro/steering/product.md`
  and the three markers to `.kiro/steering/structure.md` in the refocused prose
  style (no retired hold/TUR language — the test's `forbid` list must stay absent).
- Option B: amend the archtest's required-needle list with explicit justification
  that the invariant moved elsewhere (not recommended: the test guards durable
  project memory for the exact two-seam rule the fast path depends on).
- No change to `requirements.md` / `design.md` / `tasks.md` is required by this
  drift: the spec already states the two-seam rule correctly.

### D2 (non-material, noted for provenance)

`internal/standardplugins/provider_profile_binding.go` delta only tightens
custom-compatible backend marker validation (fail-closed composition errors for
reserved anchor/comment prefixes, unresolvable markers, cross-family builds). It
does not change route selection, candidate order, backend wire construction, or
any section-2 seam. No spec correction needed.

## 4. Validation run at implementation start

- `git diff --check`: clean (exit success, no output).
- `go test -count=1 -timeout=10m ./internal/archtest`: **FAIL** —
  single failure `TestBillingExposureDocsRejectRetiredHoldAuthorizeInvariant`
  (`billing_exposure_docs_contract_test.go:115`: `.kiro/steering/product.md`
  missing required exposure-flow marker `"cheap credit screen"`); all other
  architecture tests pass in the same run. Pre-existing at task start (caused by
  D1, committed before this task; this task made no production changes).
- Broader suites (`make test-unit`, `make quality-checks`, `make test`, `make qa`,
  `go build -trimpath ./cmd/lipstd`, plugin smoke): not run — not needed for a
  docs/evidence task; parent runs them per workstream policy.

## 5. Verdict for go/no-go on production workstreams

- Code-seam verdict: **no material code seam differs** from the spec's section-22.8
  assumption list. No `requirements.md` / `design.md` / `tasks.md` correction is
  needed before production changes on technical grounds.
- Gate verdict at implementation start: **do not claim green CI** until D1 is
  remediated (Option A). Tasks 1.2+ characterization fixtures should re-run
  `./internal/archtest` after the fix and record the green run.

## 6. Upstream repair verification (implementation-start SHA and section-4 RED preserved above)

- Repair: `caa38dc9` (`fix(steering): restore billing exposure contract`,
  cherry-picked from upstream `fix/billing-steering-contract` `a640123c`)
  restores the section-3/D1 billing-exposure contract markers in
  `.kiro/steering/product.md` and `.kiro/steering/structure.md`.
  Original implementation-start SHA (`3da34d7875443355d65cb9d7df649555dfad3edb`)
  and the section-4 historical RED
  (`TestBillingExposureDocsRejectRetiredHoldAuthorizeInvariant` failing on the
  missing `"cheap credit screen"` marker) are preserved unchanged as the
  task-start record.
- Fresh verification on the feature worktree at `caa38dc9`:
  - `go test -count=1 -timeout=10m ./internal/archtest`: **PASS**
    (`ok github.com/matdev83/go-llm-interactive-proxy/internal/archtest 53.120s`).
  - `git diff --check`: clean.
- Gate verdict after repair: architecture gate now passes; the section-5
  implementation-start block is cleared with no production changes by this task.
