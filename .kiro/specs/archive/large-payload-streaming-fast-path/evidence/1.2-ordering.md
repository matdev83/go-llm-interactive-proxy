# Task 1.2 — Frontend Outer + Shared Pipe Ordering Evidence

Task: 1.2 Freeze frontend-specific outer ordering and shared pipe ordering
(Requirements: 1, 2, 17; Design: 1, 2, 14, 16. Test-only characterization: no production changes.)

## 1. Implementation-start SHA

- HEAD: `40964029aaba8001dc94547f45a2a341fb5350c6` (branch `feat/large-payload-streaming-fast-path`, clean at start per parent brief)

## 2. Frozen oracle (design 1)

`internal/plugins/frontends/frontendpipe/pipe.go:ServeHTTP (207)`:

- keepalive ctx (209-212) -> method 405 (213) -> AltServe (217) -> MatchPath 404 (220)
- -> `reqbody.ReadAll` 413/400 (226-235) -> exec-nil 500/501 (240)
- -> header selector (245) -> optional legacy `ResolveRouteSelector(r, body, pm)` (246-250)
- -> `jsonguard.PreflightWithContext` 400/503 (251) -> single `decodeqos.TryAdmit(len(body))` 429/503 (259)
- -> `decodeqos.Guard` holding permit across `RouteFromBodyModel` defaulting + `Spec.Decode` (268-284) -> decode error 400
- -> `call.Validate`/invalid 400 -> `AfterDecode`/hook error -> traffic `LegCTP` with original body/ct (311-316) -> `spec.execute` -> `WrapStream` -> `BuildEncodeOpts` + writers.

## 3. Per-frontend facts (design 14)

- OpenAI Responses (`openairesponses/handler.go:84`): `RouteFromBodyModel=true`, no legacy resolver; `MatchPath` suffix `/responses`; `AltServe` cancel path before body.
- OpenAI Chat (`openailegacy/handler.go:64`): `RouteFromBodyModel=true`, no legacy resolver; `MatchPath` suffix `/chat/completions`.
- OpenResponses (`openresponses/handler.go:82-100`, `handler_pipe.go`): outer method 405 -> auth 401 -> JSON media 415 remain before `frontendpipe.ServeHTTP`; pipe `RouteFromBodyModel` unset (false), selector derived inside `Decode` via `resolveRouteSelector` with same header-wins rule; `AfterDecode=prepareCreateState` side effects stay post-decode.

## 4. New characterization tests (test-only, no production diff)

- `internal/plugins/frontends/frontendpipe/pipe_ordering_test.go`: success resolver-over-header order; header-wins; body-model/default fallback incl. late model; resolver-before-preflight; preflight-before-admission; admission-before-decode with Retry-After; method>path>body>exec precedence; single TryAdmit weight + Guard hold/release; AfterDecode skips traffic/execute; AltServe before path/body.
- `internal/plugins/frontends/openairesponses/ordering_characterization_test.go`: method/path/body/preflight/admission/decode precedence with production 405/404/413/400/429 + Retry-After; header-wins/body/default incl. opaque-hint bypass check; CTP traffic original body.
- `internal/plugins/frontends/openailegacy/ordering_characterization_test.go`: same matrix for `/v1/chat/completions` with chat bodies.
- `internal/plugins/frontends/openresponses/ordering_characterization_test.go`: outer 405>401>415 with wire shapes (`authentication_error/unauthorized`, `invalid_request_error/unsupported_media_type`); outer media before pipe 413/admission; pipe 404>413>400>429/503>400; header-wins/body; CTP traffic original body.

## 5. Validation

- RED sensitivity: temporary mutation `StatusMethodNotAllowed -> StatusOK` in pipe method-before-path test fails as `--- FAIL: TestPipeOrdering_MethodPathBodyExecPrecedence/method_before_path (pipe_ordering_test.go:451: status=405 want 405)`; restored before final run.
- `go test -race ...` (4 packages): unsupported on Windows — `runtime/cgo: .../cgo.exe: exit status 2` + `[build failed]` for each package; per Windows policy ran without `-race`.
- `go test -count=1 ./internal/plugins/frontends/frontendpipe/... ./internal/plugins/frontends/openairesponses/... ./internal/plugins/frontends/openailegacy/... ./internal/plugins/frontends/openresponses/...`: PASS (all 4 ok).
- `git diff --check`: clean. `gofmt -l` on 4 new files: clean.
