# Task 20.3–20.4 — Default-Off Production Rollout and Feature Closeout Evidence

Tasks covered:
- 20.3 Default-off production rollout (Requirements 20, 21, 22)
- 20.4 Completion evidence for #532/#503 (Requirements 21, 22)

---

## 1. Task 20.3: Default-Off Production Rollout Audit

### 1.1 Config Default Remains Disabled
- **Key:** `server.large_payload_fast_path.enabled`
- **Default Value:** `false` (zero-value `bool`)
- **Verification Evidence:**
  - `internal/core/config/large_payload_fast_path_test.go:TestLargePayloadFastPath_DefaultOff`: verifies `Enabled == false`, `MaxRequestBodyBytes == 0` (unmutated), and budget defaults intact.
  - `internal/core/config/large_payload_fast_path_test.go:TestLargePayloadFastPath_InvalidReloadPreservesLastGood`: verifies transactional config reload fails on invalid candidates (e.g. `threshold_bytes: 0`) and preserves the active last-good generation without mutating runtime state.
  - `internal/core/config/large_payload_fast_path_test.go:TestLargePayloadFastPath_DisabledIgnoresInvalidFields`: verifies disabled fast-path configs pass validation unconditionally (guaranteeing zero behavior change for default-off deployments).

### 1.2 Protocol Lane Advertisement & Enablement Wiring Audit
- **Requirement:** Only lanes whose 15/16/17 certification and 19 ROI gate pass may advertise wire support. NONE do: all three lanes remain canonical-only per Task 19.7.
- **Verification Evidence:**
  - **Lane 1 (OpenAI Responses):** `internal/plugins/frontends/openairesponses/e2e_lane1_conformance_test.go:TestLane1E2E_WireSupportNotAdvertised` verifies `spec.Config.LargePayload.Enabled == false` and `ThresholdBytes == 0`.
  - **Lane 2 (OpenAI Chat):** `internal/plugins/frontends/openailegacy/e2e_lane2_conformance_test.go:TestLane2E2E_WireSupportNotAdvertised` verifies `spec.Config.LargePayload.Enabled == false` and `ThresholdBytes == 0`.
  - **Lane 3 (OpenResponses):** `internal/plugins/frontends/openresponses/e2e_lane3_conformance_test.go:TestLane3E2E_WireSupportNotAdvertised` verifies `spec.Config.LargePayload.Enabled == false` and `ThresholdBytes == 0`.
  - **Zero Enablement Wiring in Production:** Grep audit across `cmd/lipstd`, `internal/stdhttp`, and `internal/infra/runtimebundle` confirms zero occurrences of `LargePayload` enablement wiring. Stock binaries compose canonical handlers only.

### 1.3 Unknown / New Runtime Authority Fails Closed to Canonical
- **Requirement:** Any unknown or unclassified runtime authority must fail closed to canonical execution.
- **Verification Evidence (Group 3 Ratchets verified in 20.1):**
  - `internal/core/largebody/eligibility_test.go:TestWireEligibility_UnknownFailsClosed`: verifies that unknown planes, duplicate planes, missing planes, unclassified access, out-of-range access, and empty generation IDs return unsealed, blocking summaries that fail validation.
  - `internal/core/largebody/eligibility_test.go:TestWireEligibility_ZeroValueFailsClosed`: verifies that uninitialized zero summaries are unsealed, blocking, and unpinned.
  - `internal/core/largebody/authority_gate_test.go`: verifies assessment rejects unknown or unclassified authorities pre-commit.
  - `internal/archtest/` wire boundary tests (Task 20.1): enforce zero unauthorized `lipapi.Call` or `CloneCall` post-commit references.

### 1.4 Operator Documentation
- **File:** `docs/large-payload-fast-path.md`
- **Sections Covered:**
  - Full-body prevalidation (V1 receives/validates full payload before provider open; heap/GC optimization, not TTFT).
  - Spool confidentiality (plaintext prompt data, `spool_dir` volume placement, 0600 permissions, random filenames, immediate deletion, telemetry redaction).
  - Memory-vs-I/O tradeoffs (`threshold_bytes`, `memory_spool_bytes`, `max_inflight_spool_bytes`, `max_semantic_fact_bytes`; optimization budget vs OOM admission; flat ~35.6 KiB retained heap vs ~1.4-1.6x latency).
  - Metrics and decline reasons (`lip_large_payload_*` metrics family, closed-enum decline reasons).
  - Rollback toggle and transactional reload (`server.large_payload_fast_path.enabled: false`, SIGHUP/admin reload, last-good preservation).

---

## 2. Task 20.4: Completion Evidence Audit for #532 / #503

Every required artifact category is verified and mapped to a real tracked repository artifact:

| Evidence Category | Artifact / Path | Status | Key Findings / Metrics |
|---|---|---|---|
| **Checked-in Start SHA + Baselines** | `evidence/1.1-rebaseline.md`, `evidence/1.10-perf-baseline.md`, `evidence/1.2-ordering.md` | Verified | Start SHA `b08c608` rebaselined to `caa38dc9`; canonical baseline performance across 32 KiB, 256 KiB, 1 MiB, 5 MiB, 20 MiB; phase ordering characterized. |
| **Authority / Call Census** | `evidence/1.8-call-census.md`, `evidence/12.1-runtime-wire-facts.md` | Verified | 46 narrow-port inventory, Call read/clone classifications, 8 bounded wire fact domains, zero shadow Call. |
| **Plane / Non-Plane Matrix** | `evidence/1.9-plane-census.md`, `evidence/19.6-19.7-eligibility-roi.md` | Verified | 26 typed extension planes (19 CanonicalRequired, 4 ResponseOnly, 3 MetadataOnly, 0 Unclassified); 4 hook chains; non-negotiable blocker planes (`local_turn`, `secret_guard`, `terminal_decision`). |
| **Differential Protocol Evidence** | `evidence/15.2-red-phase.md`, `evidence/15.3-red-phase.md`, `internal/plugins/frontends/openairesponses/e2e_lane1_conformance_test.go` | Verified | Lane 1: 14/14 E2E conformance green, two-leg canonical vs wire differential, model rewrite splice. |
| **Transport Evidence** | `internal/plugins/backends/openaicompat/wire_open_test.go`, E2E test suites | Verified | HTTP/1.1 and HTTP/2.0 wire conformance, connection reuse, framing header stripping, exact Content-Length. |
| **Identity Parity Evidence** | `evidence/14.2-red-phase.md`, `internal/core/largebody/identity_differential_test.go`, `identity_fuzz_test.go` | Verified | Stable Call ID/token/timestamp hash parity, `CallIdentityWriter` streaming hash, carrier cancellation ID binding. |
| **Secure-Session Evidence** | `evidence/14.3-red-phase.md`, `internal/core/largebody/carrier_telemetry_redaction_test.go`, `internal/plugins/frontends/sessionwire/` | Verified | Turn-2 session resume parity, `SensitiveString` redaction in logs/metrics/traces, normalized `ClientTurnShape` recorder input. |
| **Economic & Metering Evidence** | `evidence/12.1-runtime-wire-facts.md`, `internal/core/largebody/economic_identity_parity_test.go`, `internal/core/runtime/wire_metering_composition_test.go` | Verified | Bounded `fe-ingress` and `be-ingress` checkpoints, widening assertion, exact wire token counting, zero Call cloning. |
| **Retry / Race / Cancel Evidence** | `evidence/14.4-red-phase.md`, `internal/core/runtime/task13_3_recovery_matrix_test.go` | Verified | Attempt 1 failover to Attempt 2 from offset 0, reader independence, no retry after first client byte, context cancellation cleanup. |
| **Resource Safety Evidence** | `evidence/19.1-red-phase.md`, `internal/core/largebody/spool_test.go`, `spill_test.go`, `fault_privacy_test.go` | Verified | Bounded RAM spool, 0600 file permissions, unpredictable filenames, pending deletion on Windows, lossless canonical continuation on write faults. |
| **Heap-Scaling Evidence** | `evidence/19.2-19.5-benchmarks.md` | Verified | Post-commit retained Go heap is flat **35,656 B/op (0.000% slope)** across 1 MiB, 5 MiB, 20 MiB; honest mixed 19.3 verdict documenting transient proof-time allocation bottleneck (~6.5-7.9x payload). |
| **Static Blocker Overhead** | `evidence/19.2-19.5-benchmarks.md` §6, `evidence/19.6-19.7-eligibility-roi.md:192` | Verified | Pre-capture static disposition check takes **11–14 ns/op** (vs ~4–5 ns baseline) with **0 allocs**, 0 temp files, 0 scanners. |
| **Lane ROI Decisions** | `evidence/19.6-19.7-eligibility-roi.md` | Verified | All 3 certified protocol lanes remain canonical-only in production due to transient proof heap spike and lack of production assessor composition. |
| **QA / Architecture Ratchets** | Tasks 20.1–20.2 (commit `5ac3b6ef`) | Verified | All 10 architecture ratchets pass (no unclassified planes, no provider names in core, no fake Calls, no post-commit Call ingress); repo tests clean. |

---

## 3. GitHub Issue #532 / #503 Closeout Readiness

- **Status:** **READY FOR CLOSEOUT** upon PR merge.
- **Action:** No GitHub writes performed directly (per instructions). Readiness recorded here for the parent controller / PR delivery agent.
- **Follow-Up Workstreams (Intentionally Canonical-Only in V1):**
  1. **Decoded-Gzip Replay (Task 18.2):** Compressed payloads bypass fast-path capture to canonical decoding per Task 18.1. Decoded-gzip replay deferred until justified by future ROI evidence.
  2. **OpenResponses Storage & Continuation (Task 17.4):** Fast-path restricted to explicit `store: false` without `previous_response_id`. `store: true`, continuations, compaction, and WebSocket remain canonical.
  3. **Streaming `CompileProof` Rework:** Replace transient `io.ReadAll` in `CompileProof` with streaming token scanning and `CallIdentityWriter` to eliminate proof-time $O(\text{payload})$ heap spike (closing Task 19.3 gate).
  4. **Production Assessor Composition:** Unify `AuthorityAssessor`, `BackendWireProofAssessor`, `RouteOverrideAssessor`, and `runtime.Executor` into a production `LargeBodyAssessor` implementation composed in `runtimebundle.BuildHost`.
  5. **Enablement Wiring:** Formally expose `server.large_payload_fast_path` configuration to frontends in `runtimebundle` and allow advertising support once activation conditions are met.
  6. **Detached-Session Execution (Req 19.8):** Detached execution remains canonical-only until its Call/lifecycle dependencies are separately represented and parity-tested.
  7. **Legacy `ResolveRouteSelector` Contract (Req 13.2):** A configured full-body route resolver remains canonical-only in V1; a future bounded route-resolution contract must preserve ordering, precedence, and selector semantics.
