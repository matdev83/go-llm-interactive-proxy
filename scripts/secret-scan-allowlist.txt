# Optional path exclusions for scripts/check-staged-secrets.* (built-in git grep pass).
# One pathspec per line (relative to repo root), passed as :(exclude)<line>
# Example:
# internal/testkit/fixtures/wire_samples/
