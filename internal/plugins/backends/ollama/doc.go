// Package ollama implements the Ollama backend connector over the OpenAI-compatible
// /v1 surface with optional Responses support (Ollama >= 0.13.3), local/cloud model
// discovery, and conservative capability mapping from /api/show probes.
package ollama

const ID = "ollama"

const CloudID = "ollama-cloud"
