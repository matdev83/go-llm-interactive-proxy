// Package fakebridge provides a deterministic Cursor SDK bridge harness for
// Go tests. It scripts startup, models, events, malformed/oversized frames,
// blocked cancel, stderr, exit, and shutdown without Node or network access.
package fakebridge
